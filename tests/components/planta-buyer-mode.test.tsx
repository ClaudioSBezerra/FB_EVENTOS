// FB_EVENTOS — FORN-03: Konva planta buyer-mode click filter (Wave 0 scaffold, Plan 02-01).
// Filled in by Plan 02-03. See VALIDATION.md row FORN-03.
//
// TODO (Plan 02-03): import { PlantaEditor } from '@/components/eventos/planta-editor'

import { describe, it } from 'vitest';

describe('FORN-03: planta-editor buyer mode', () => {
  it.todo('mode="buyer" blocks click on sold lots');
  it.todo('mode="buyer" blocks click on reserved lots');
  it.todo('mode="buyer" allows click on available lots');
  it.todo('mode="admin" allows clicks regardless of lot status (no regression)');
});
