---
phase: 02-fornecedor-self-service-checkout-pix-cartao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/event_addons.ts
  - src/db/schema/cart_addon_lines.ts
  - src/db/schema/lot_reservations.ts
  - src/db/schema/lot_waitlist.ts
  - src/db/schema/outbox_events.ts
  - src/db/schema/payment_webhooks_inbox.ts
  - src/db/schema/vendor_consents.ts
  - src/db/schema/refund_requests.ts
  - src/db/schema/tenants.ts
  - src/db/schema/index.ts
  - src/db/migrations/0017_phase2_domain_tables.sql
  - src/db/migrations/0018_phase2_force_rls.sql
  - src/db/migrations/0019_phase2_migrator_select_policies.sql
  - src/db/migrations/0020_tenants_phase2_columns.sql
  - tests/test-mocks/pagarme.ts
  - tests/test-mocks/graphile-worker.ts
  - tests/fornecedor/signup.test.ts
  - tests/marketplace/list.test.ts
  - tests/components/planta-buyer-mode.test.tsx
  - tests/reservations/create.test.ts
  - tests/reservations/concurrent.test.ts
  - tests/jobs/reservation-expire.test.ts
  - tests/sse/route.test.ts
  - tests/cart/total.test.ts
  - tests/payments/checkout-paths.test.ts
  - tests/webhooks/pagarme-idempotent.test.ts
  - tests/webhooks/hmac-verify.test.ts
  - tests/webhooks/perf.test.ts
  - tests/outbox/atomic.test.ts
  - tests/outbox/saga-cancel.test.ts
  - tests/waitlist/notify-and-consume.test.ts
  - tests/refunds/end-to-end.test.ts
  - tests/portal/render.test.ts
  - tests/lgpd/vendor-consent.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 8 Phase 2 schemas exist in src/db/schema/ and re-export from index.ts"
    - "Wave-0 test scaffolds for every FORN-XX exist (18 .test.ts files) — each contains at minimum a placeholder describe/it block that imports the (yet-unwritten) source-of-truth module and asserts a known-failing expectation, so vitest runs them red"
    - "tests/test-mocks/pagarme.ts and tests/test-mocks/graphile-worker.ts exist with stub MSW handlers + in-process task harness"
    - "Migrations 0017..0020 apply cleanly via pnpm db:migrate against fresh DB; pnpm db:check shows no drift"
    - "pnpm check:drizzle passes (no drizzle-kit push usage)"
    - "Operator CHECKPOINT: review generated SQL diff before commit, since drizzle-kit generate may detect renames interactively"
  artifacts:
    - path: "src/db/schema/event_addons.ts"
      provides: "eventAddons table per D-01 (event_id FK + price_brl_cents + max_qty + active)"
      contains: "export const eventAddons"
    - path: "src/db/schema/cart_addon_lines.ts"
      provides: "cartAddonLines table joining lot_reservations to event_addons"
      contains: "export const cartAddonLines"
    - path: "src/db/schema/lot_reservations.ts"
      provides: "lotReservations TTL table per FORN-04 (expires_at + released_at + payment_method nullable)"
      contains: "export const lotReservations"
    - path: "src/db/schema/lot_waitlist.ts"
      provides: "lotWaitlist FIFO per D-11"
      contains: "export const lotWaitlist"
    - path: "src/db/schema/outbox_events.ts"
      provides: "outboxEvents single-table discriminator per D-16 + AM-03"
      contains: "export const outboxEvents"
    - path: "src/db/schema/payment_webhooks_inbox.ts"
      provides: "paymentWebhooksInbox idempotency table per D-14 (gateway_event_id PK + tenant_id denorm per Open Q4)"
      contains: "export const paymentWebhooksInbox"
    - path: "src/db/schema/vendor_consents.ts"
      provides: "vendorConsents per D-24 (consent_type enum, ip_address PII)"
      contains: "export const vendorConsents"
    - path: "src/db/schema/refund_requests.ts"
      provides: "refundRequests FSM table per D-07/D-08+AM-04"
      contains: "export const refundRequests"
    - path: "src/db/migrations/0017_phase2_domain_tables.sql"
      provides: "CREATE TABLE for all 8 tables + ENABLE RLS"
      contains: "CREATE TABLE \"lot_reservations\""
    - path: "src/db/migrations/0018_phase2_force_rls.sql"
      provides: "ALTER TABLE ... FORCE ROW LEVEL SECURITY + COMMENT ON COLUMN PII + GRANTs to fb_eventos_app"
      contains: "FORCE ROW LEVEL SECURITY"
    - path: "src/db/migrations/0019_phase2_migrator_select_policies.sql"
      provides: "SELECT-only policies for fb_eventos_migrator on payment_webhooks_inbox + outbox_events + lot_reservations"
      contains: "CREATE POLICY"
    - path: "src/db/migrations/0020_tenants_phase2_columns.sql"
      provides: "ALTER tenants add vendor_auto_approve + refund_policy_json"
      contains: "ALTER TABLE \"tenants\""
  key_links:
    - from: "src/db/schema/index.ts"
      to: "all 8 new schemas"
      via: "barrel re-export"
      pattern: "export \\* from './(event_addons|cart_addon_lines|lot_reservations|lot_waitlist|outbox_events|payment_webhooks_inbox|vendor_consents|refund_requests)'"
    - from: "src/db/migrations/0017"
      to: "fb_eventos_app role"
      via: "GRANT in 0018"
      pattern: "GRANT SELECT, INSERT, UPDATE, DELETE ON \".*\" TO fb_eventos_app"
---

<objective>
Wave 0 phase scaffold — install all Phase 2 schemas, the four migrations (0017..0020), and the 18 failing test files that Waves 2..7 will turn green. This is the foundation slice: it creates no end-user behavior, but ALL downstream plans depend on these tables existing and these test files compiling. It is intentionally the only plan that does not satisfy the strict MVP vertical-slice rule — Phase 2 cannot ship a single feature without the 8 new tables, and the rest of the plans rely on Wave 0 test infra per RESEARCH §Wave 0 Gaps.

Purpose: Bootstrap Phase 2 with a green DB schema + an unbroken-failing test grid. Operator CHECKPOINT reviews the drizzle-kit generated SQL before commit.

Output: 8 schema files, 4 migrations, schema barrel update, 18 failing test scaffolds, 2 mocks (pagarme + graphile-worker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-VALIDATION.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md
@src/db/schema/index.ts
@src/db/schema/lots.ts
@src/db/schema/vendors.ts
@src/db/schema/contracts.ts
@src/db/schema/payments.ts
@src/db/schema/tenants.ts
@src/db/schema/consent.ts
@src/db/schema/audit.ts
@src/db/migrations/0011_phase1_force_rls.sql
@src/db/migrations/0014_zapsign_webhook_tenant_lookup.sql
@src/db/migrations/0015_pagarme_webhook_tenant_lookup.sql
@src/db/migrations/0016_tenant_platform_commission.sql
@drizzle.config.ts

<interfaces>
<!-- Patterns the executor copies verbatim from 02-PATTERNS.md §Group B. -->
<!-- See 02-PATTERNS.md lines 472-505 (lot_categories analog for event_addons), -->
<!-- lines 518-547 (lot_assignments analog for lot_reservations + partial unique), -->
<!-- lines 562-586 (zapsign_documents analog for payment_webhooks_inbox), -->
<!-- lines 595-616 (audit_log analog for outbox_events), -->
<!-- lines 625-645 (consentRecords analog for vendor_consents). -->

Migration 0017 follows 0010 pattern: drizzle-kit generate from schema + hand-edited additions for FORCE/COMMENT/GRANT in 0018.
Migration 0019 mirrors 0014: SELECT-only policy on fb_eventos_migrator for outbox/inbox/reservations cross-tenant scans.
Migration 0020 mirrors 0016: ALTER tenants ADD COLUMN vendor_auto_approve boolean NOT NULL DEFAULT false + refund_policy_json jsonb (nullable, default tier table embedded via separate UPDATE if needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 8 Phase 2 Drizzle schemas + tenant ALTER + barrel update</name>
  <files>src/db/schema/event_addons.ts, src/db/schema/cart_addon_lines.ts, src/db/schema/lot_reservations.ts, src/db/schema/lot_waitlist.ts, src/db/schema/outbox_events.ts, src/db/schema/payment_webhooks_inbox.ts, src/db/schema/vendor_consents.ts, src/db/schema/refund_requests.ts, src/db/schema/tenants.ts, src/db/schema/index.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (§Group B lines 26-39 + per-table excerpts lines 472-645)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-01, D-11, D-14, D-16, D-22, D-24)
    - src/db/schema/lots.ts (lotCategories analog — full file)
    - src/db/schema/vendors.ts (lotAssignments partial-unique analog — full file)
    - src/db/schema/contracts.ts (zapsignDocuments gateway-id pattern — full file)
    - src/db/schema/audit.ts (auditLog append-only pattern — full file)
    - src/db/schema/consent.ts (consentRecords pattern — full file)
    - src/db/schema/tenants.ts (existing columns — full file)
    - src/db/schema/index.ts (barrel export pattern — full file)
  </read_first>
  <action>
    Create the 8 new schema files following the per-table analog spec in 02-PATTERNS.md.

    **event_addons.ts (per D-01):** table `event_addons`; columns id (uuid pk default random), tenantId (uuid not null FK tenants.id), eventId (uuid not null FK events.id), name (text not null), priceBrlCents (integer not null), maxQty (integer not null default 1), active (boolean not null default true), createdAt/updatedAt/deletedAt timestamps. Indices on tenantId, eventId, partial on active=true. RLS policy tenant_isolation via fbEventosApp role using current_setting('app.current_tenant_id',true)::uuid. Call .enableRLS().

    **cart_addon_lines.ts (per D-01):** table `cart_addon_lines`; columns id, tenantId, reservationId (uuid not null FK lot_reservations.id ON DELETE CASCADE), addonId (uuid not null FK event_addons.id), quantity (integer not null default 1), priceBrlCentsSnapshot (integer not null — preserve price at reserve-time), createdAt. Indices on tenantId + reservationId. Same RLS pattern.

    **lot_reservations.ts (per FORN-04 + D-05 AM-01 simplified):** table `lot_reservations`; columns id, tenantId, lotId (uuid not null FK lots.id), vendorId (uuid not null FK vendors.id), eventId (uuid not null FK events.id — denormalized for advisory-lock key + SSE channel), reservedAt (timestamptz default now), expiresAt (timestamptz not null), releasedAt (timestamptz nullable), paymentMethod (text nullable; values 'pix'|'credit_card'). Indices on tenantId, vendorId, expiresAt, partial unique on lotId WHERE released_at IS NULL AND expires_at > now() (declared in migration since drizzle does not emit partial uniques). Same RLS pattern.

    **lot_waitlist.ts (per D-11):** table `lot_waitlist`; columns id, tenantId, lotId, eventId (denorm for fast notify scoping), vendorId, joinedAt (default now), notifiedAt (nullable), tokenJti (uuid nullable — minted at notify time for single-use enforcement). Indices on tenantId, (lotId, joinedAt) for RANK computations. Same RLS.

    **outbox_events.ts (per D-16 + AM-03):** table `outbox_events`; columns id, tenantId, eventType (text not null — values constrained in migration via CHECK: 'payment.created'|'payment.paid'|'payment.failed'|'lot.reserved'|'lot.sold'|'lot.released'|'lot.status_changed'|'refund.created'), aggregateId (uuid not null), payload (jsonb not null), createdAt, processedAt (nullable), processingStatus (text not null default 'pending', CHECK in 'pending'|'processed'|'failed'), attemptCount (integer not null default 0). Indices on tenantId, eventType, partial on (createdAt) WHERE processed_at IS NULL AND processing_status != 'failed' (the drain hot path). Same RLS.

    **payment_webhooks_inbox.ts (per D-14 + AM-02 + Open Q4):** table `payment_webhooks_inbox`; columns gatewayEventId (text PRIMARY KEY — Pagar.me event id; NOT a uuid), tenantId (uuid not null — denormalized at INSERT-time so worker enters withTenant per Open Q4), eventType (text not null), payload (jsonb not null — raw body), receivedAt (default now), processedAt (nullable), processingStatus (text default 'pending'). Index on tenantId, (processingStatus, receivedAt). Same RLS.

    **vendor_consents.ts (per D-24):** table `vendor_consents`; columns id, tenantId, vendorId (uuid not null FK vendors.id), consentType (text not null — CHECK constraint in migration: marketing|analytics|payment_data), grantedAt (default now), revokedAt (nullable), ipAddress (text nullable — PII), consentText (text — snapshot), consentVersion (text not null). Index on tenantId + vendorId. Same RLS.

    **refund_requests.ts (per D-07/D-08+AM-04):** table `refund_requests`; columns id, tenantId, paymentId (uuid not null FK payments.id), vendorId (uuid not null FK vendors.id), requestedAt (default now), refundPct (numeric(5,2) not null), refundAmountBrlCents (integer not null), reason (text nullable — may carry PII), status (text not null default 'pending', CHECK in 'pending'|'processing'|'completed'|'failed'), pagarmeRefundId (text nullable — DELETE response charge id), completedAt (nullable), failureReason (text nullable). Index on tenantId, paymentId. Same RLS.

    **tenants.ts EXTEND (per D-23 + D-07 escape hatch):** add `vendorAutoApprove: boolean('vendor_auto_approve').notNull().default(false)` and `refundPolicyJson: jsonb('refund_policy_json')` (nullable — null means use DEFAULT_POLICY constant from src/lib/refund/policy.ts).

    **index.ts EXTEND:** add `export * from './event_addons'`, `'./cart_addon_lines'`, `'./lot_reservations'`, `'./lot_waitlist'`, `'./outbox_events'`, `'./payment_webhooks_inbox'`, `'./vendor_consents'`, `'./refund_requests'` (alphabetical-by-filename to match existing convention).

    Verify all 8 schemas pass `pnpm tsc --noEmit` and the barrel re-exports compile.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm tsc --noEmit &amp;&amp; grep -c "^export \* from './" src/db/schema/index.ts | xargs -I{} test {} -ge 19</automated>
  </verify>
  <done>All 8 new schema files exist with .enableRLS() called; index.ts re-exports all 8; tenants.ts has vendorAutoApprove + refundPolicyJson; `pnpm tsc --noEmit` exits 0; the barrel has at least 19 export-from lines (11 existing + 8 new).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Generate Drizzle migrations 0017..0020 + operator review of SQL diff</name>
  <files>src/db/migrations/0017_phase2_domain_tables.sql, src/db/migrations/0018_phase2_force_rls.sql, src/db/migrations/0019_phase2_migrator_select_policies.sql, src/db/migrations/0020_tenants_phase2_columns.sql, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - drizzle.config.ts (full file)
    - src/db/migrations/0010_phase1_domain_tables.sql (analog for drizzle-kit-generated CREATE TABLEs — full file)
    - src/db/migrations/0011_phase1_force_rls.sql (analog for FORCE + COMMENT + GRANT — full file)
    - src/db/migrations/0014_zapsign_webhook_tenant_lookup.sql (analog for SELECT-only policy on fb_eventos_migrator — full file)
    - src/db/migrations/0015_pagarme_webhook_tenant_lookup.sql (analog — full file)
    - src/db/migrations/0016_tenant_platform_commission.sql (analog for tenant column ALTER — full file)
  </read_first>
  <what-built>
    Migration 0017 generated by `pnpm db:generate` from the 8 new schema files (drizzle-kit emits CREATE TABLE + ENABLE RLS + tenant_isolation policy + indices automatically).

    Hand-authored 0018 mirroring 0011 shape:
    - `ALTER TABLE ... FORCE ROW LEVEL SECURITY` for every new tenant-scoped table (8 ALTERs)
    - `GRANT SELECT, INSERT, UPDATE, DELETE ON ... TO fb_eventos_app` for each
    - `COMMENT ON COLUMN` for PII columns: `vendor_consents.ip_address` 'PII: LGPD low-sensitivity; consent IP', `refund_requests.reason` 'PII: free-text — may contain CNPJ/email', `lot_waitlist.token_jti` 'PII: not by itself; but ties vendor to lot release notification'
    - CHECK constraints: `outbox_events.event_type IN (...)`, `outbox_events.processing_status IN (...)`, `vendor_consents.consent_type IN (...)`, `refund_requests.status IN (...)`
    - Partial unique on `lot_reservations`: `CREATE UNIQUE INDEX lot_reservations_lot_id_active_unique ON lot_reservations(lot_id) WHERE released_at IS NULL AND expires_at &gt; now()`

    Hand-authored 0019 mirroring 0014:
    - `CREATE POLICY "webhook_tenant_lookup_migrator_read" ON payment_webhooks_inbox AS PERMISSIVE FOR SELECT TO fb_eventos_migrator USING (true);`
    - `GRANT SELECT ON payment_webhooks_inbox TO fb_eventos_migrator;`
    - Similar for outbox_events (SELECT + UPDATE on processed_at, processing_status, attempt_count) + lot_reservations (SELECT + UPDATE on released_at) so cross-tenant scheduled tasks work via migratorPool.

    Hand-authored 0020 mirroring 0016:
    - `ALTER TABLE tenants ADD COLUMN IF NOT EXISTS vendor_auto_approve boolean NOT NULL DEFAULT false;`
    - `ALTER TABLE tenants ADD COLUMN IF NOT EXISTS refund_policy_json jsonb;`

    Then ran `pnpm db:migrate` against fresh test Postgres + `pnpm db:check` (zero drift).
  </what-built>
  <how-to-verify>
    Operator (human) reviews:
    1. The SQL diff in 0017 looks like a normal drizzle-kit output with no surprise DROP or rename detection
    2. 0018 lists every new tenant-scoped table in the FORCE block (8 ALTERs)
    3. 0019 grants SELECT to fb_eventos_migrator ONLY on the cross-tenant-scan tables (3 tables — inbox, outbox, reservations) and nothing else
    4. 0020 uses ADD COLUMN IF NOT EXISTS (re-runnable)

    Run locally:
    ```
    pnpm db:generate
    pnpm db:migrate
    pnpm db:check       # exits 0 — no schema drift
    pnpm check:drizzle  # exits 0 — no drizzle-kit push in source
    ```

    Then approve the SQL diff. If drizzle-kit prompts interactively for a rename detection, DO NOT auto-resolve — abort, screenshot the prompt for the operator, and re-run with the manual SQL hand-edit instead.
  </how-to-verify>
  <resume-signal>Type "approved 02-01 migrations" or describe required edits.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 18 Wave-0 failing test scaffolds + 2 test mocks (pagarme + graphile-worker)</name>
  <files>tests/test-mocks/pagarme.ts, tests/test-mocks/graphile-worker.ts, tests/fornecedor/signup.test.ts, tests/marketplace/list.test.ts, tests/components/planta-buyer-mode.test.tsx, tests/reservations/create.test.ts, tests/reservations/concurrent.test.ts, tests/jobs/reservation-expire.test.ts, tests/sse/route.test.ts, tests/cart/total.test.ts, tests/payments/checkout-paths.test.ts, tests/webhooks/pagarme-idempotent.test.ts, tests/webhooks/hmac-verify.test.ts, tests/webhooks/perf.test.ts, tests/outbox/atomic.test.ts, tests/outbox/saga-cancel.test.ts, tests/waitlist/notify-and-consume.test.ts, tests/refunds/end-to-end.test.ts, tests/portal/render.test.ts, tests/lgpd/vendor-consent.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-VALIDATION.md (Wave 0 list + REQ→test map — full file)
    - tests/payments/pagarme-create.test.ts (existing Phase 1 test pattern — full file)
    - tests/payments/pagarme-webhook.test.ts (existing Phase 1 test pattern — full file)
    - tests/test-utils/dual-tenant.ts (cross-tenant pattern reused — full file)
    - src/test/external-mocks.ts (existing MSW server pattern — full file)
  </read_first>
  <action>
    Create the 18 failing-but-compiling test files matching the VALIDATION.md REQ→test map verbatim.

    **For each test file:** write at least ONE `describe('FORN-XX', () => { it.todo('behavior text from VALIDATION.md') })` block (vitest's `it.todo` makes the test red without making it crash). Reference the source-of-truth module the test will eventually drive (e.g. `tests/reservations/create.test.ts` imports nothing yet but has `// TODO: import { reserveLotInTenant } from '@/lib/actions/reservations' once Plan 02-03 lands`). The point is to give the executor in each downstream plan an existing file to fill in, not to leave the file empty.

    **tests/test-mocks/pagarme.ts:** Extend the existing pattern in src/test/external-mocks.ts. Export `createPagarmeMswHandlers(opts)` returning an array of MSW handlers for: POST /core/v5/orders (PIX, credit_card with installments echo), DELETE /core/v5/charges/:id (with optional amount body for partial refunds — AM-04), GET /core/v5/orders/:id (re-fetch defense), webhook event payload fixtures. Handlers stub realistic Pagar.me response shapes (qr_code, qr_code_url, installment_amount echo, charge.status FSM transitions). HMAC signature header generation helper is included (algorithm SHA-256 base64 per ME parent platform — to be confirmed at probe-test time in Plan 02-05).

    **tests/test-mocks/graphile-worker.ts:** In-process task harness. Export `runTaskInline(taskName, payload, helpers?)` that looks up the task in `src/jobs/tasks/index.ts::taskList` and executes the function directly (no actual worker pool). Provides deterministic scheduled-task testing — Plan 02-03 will use it for `reservation.expire` and Plan 02-06 for `outbox.drain`.

    All 18 test files are committed RED (it.todo doesn't fail; that's fine — vitest reports them yellow/todo and they will be filled in by downstream plans). The point is the file PATH exists so subsequent commits don't need to create new test files — they fill them in.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm test --run 2>&amp;1 | tail -10 &amp;&amp; ls tests/fornecedor/signup.test.ts tests/reservations/concurrent.test.ts tests/webhooks/hmac-verify.test.ts tests/outbox/atomic.test.ts tests/waitlist/notify-and-consume.test.ts tests/refunds/end-to-end.test.ts tests/portal/render.test.ts tests/lgpd/vendor-consent.test.ts tests/test-mocks/pagarme.ts tests/test-mocks/graphile-worker.ts 2>&amp;1 | wc -l | xargs -I{} test {} -eq 10</automated>
  </verify>
  <done>All 18 test files exist; all 2 test mocks exist; `pnpm test --run` exits 0 (it.todo is not a failure); baseline vitest counter shows 181 passing + N todo (N ≥ 18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CI | Drizzle-kit generate output crosses into the migrations dir — must be reviewable in PR |
| migrator role → app role | DDL run by `fb_eventos_migrator`; runtime DML uses NOBYPASSRLS `fb_eventos_app` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | drizzle-kit generated SQL | mitigate | Task 2 is `checkpoint:human-verify gate="blocking"` — operator reviews SQL diff before commit. Rename detection auto-prompt aborts the task. |
| T-02-02 | Information disclosure | new tenant-scoped tables w/o FORCE RLS | mitigate | Migration 0018 lists ALL 8 new tables in the FORCE block + GRANTs to NOBYPASSRLS `fb_eventos_app` (Phase 0 invariant) |
| T-02-03 | Information disclosure | cross-tenant access via migratorPool on inbox/outbox/reservations | mitigate | Migration 0019 grants SELECT-only (or SELECT+UPDATE on specific columns) to `fb_eventos_migrator` — narrowest possible grant for the cross-tenant-scan workers |
| T-02-04 | Information disclosure | PII columns (vendor_consents.ip_address, refund_requests.reason) | mitigate | COMMENT ON COLUMN 'PII:' inventory in Migration 0018 — same Phase 0 pattern carried forward |
| T-02-05 | Tampering | drizzle-kit push bypass | mitigate | `pnpm check:drizzle` CI gate (Phase 0 Plan 02); Wave 0 verifies it still passes |
| T-02-06 | Denial of service | unbounded outbox.processed_at growth | accept | Phase 4 will add a retention job; Phase 2 piloto volume is sub-100k rows — no problem |
| T-02-SC | Tampering | No new npm packages installed in this plan | n/a | jose is added in Plan 02-07 with checkpoint:human-verify; this plan has zero npm changes |
</threat_model>

<verification>
- `pnpm tsc --noEmit` → exits 0
- `pnpm db:generate` → emits 0017..0020 SQL files
- `pnpm db:migrate` → applies cleanly to local Postgres
- `pnpm db:check` → exits 0 (no drift)
- `pnpm check:drizzle` → exits 0 (no drizzle-kit push in src)
- `pnpm check:all` → all gates green
- `pnpm test --run` → 181 passing + ≥18 todos (no regressions); 0 failures
- All 18 test files + 2 mocks exist on disk
</verification>

<success_criteria>
- 8 new schemas exist, exported via index.ts barrel
- 4 migrations exist, apply cleanly, no drift
- 18 Wave-0 test files exist (it.todo blocks); 2 mocks exist
- CI gates remain green; no regressions in 181 Phase 1 tests
- Operator approved the SQL diff at the [BLOCKING] CHECKPOINT
</success_criteria>

<output>
Create `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-01-SUMMARY.md` when done. Include the auto-generated drizzle-kit diff hash + the operator approval timestamp.
</output>
