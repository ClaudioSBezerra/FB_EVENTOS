---
phase: 02-fornecedor-self-service-checkout-pix-cartao
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/lib/actions/reservations.ts
  - src/lib/validators/reservations.ts
  - src/lib/outbox/emit.ts
  - src/components/eventos/planta-editor.tsx
  - src/app/[slug]/marketplace/[eventId]/planta/page.tsx
  - src/jobs/tasks/reservation-expire.ts
  - src/jobs/tasks/index.ts
  - src/jobs/runner.ts
  - tests/reservations/create.test.ts
  - tests/reservations/concurrent.test.ts
  - tests/jobs/reservation-expire.test.ts
  - tests/components/planta-buyer-mode.test.tsx
  - tests/outbox/atomic.test.ts
autonomous: true
requirements:
  - FORN-03
  - FORN-04
  - FORN-05
  - FORN-06
  - FORN-13
must_haves:
  truths:
    - "Fornecedor on /{slug}/marketplace/{eventId}/planta sees a Konva canvas with planta image background + colored polygons (green=available, grey=reserved-by-other, red=sold). Clicks on green polygons open a CheckoutSidebar stub; sold/reserved polygons have cursor:not-allowed and do not fire callbacks."
    - "Concurrent reservation attempts on the same lot: pg_try_advisory_xact_lock + partial-unique index ensure exactly 1 winner; the loser receives a 409-equivalent typed error message 'Lote já reservado por outro fornecedor'"
    - "A successful reserveLotInTenant call atomically inserts lot_reservations row + outbox_events row 'lot.reserved' inside the SAME withTenant transaction; if either fails, neither persists"
    - "Graphile-Worker crontab schedules reservation.expire every 1 min; the task UPDATEs released_at=now() for rows where expires_at < now() AND released_at IS NULL, and emits lot.released outbox event per released row"
    - "Reservation TTL is 15 min hard-coded (per D-05 simplified by AM-01 — boleto deferred)"
  artifacts:
    - path: "src/lib/actions/reservations.ts"
      provides: "reserveLotInTenant pure helper + reserveLot thin Server Action; advisory-lock-then-INSERT pattern per 02-PATTERNS.md §reservations + RESEARCH §Pattern 1"
      contains: "pg_try_advisory_xact_lock"
    - path: "src/lib/outbox/emit.ts"
      provides: "emitOutboxEvent(db, eventType, aggregateId, payload) + emitOutboxEventAndNotify variant (same-tx pg_notify for SSE per AM-03)"
      contains: "INSERT INTO outbox_events"
    - path: "src/components/eventos/planta-editor.tsx"
      provides: "Added 'buyer' mode; click filtering by status; onLotClicked callback; SSE EventSource subscription (URL set; handler ships in Plan 02-04)"
      contains: "mode === 'buyer'"
    - path: "src/app/[slug]/marketplace/[eventId]/planta/page.tsx"
      provides: "Buyer-mode planta page mounts PlantaEditor mode='buyer' + opens CheckoutSidebar stub on lot click"
    - path: "src/jobs/tasks/reservation-expire.ts"
      provides: "Scheduled task per FORN-06 — cross-tenant scan via migratorPool + withTenant per row + emit lot.released"
      contains: "RESERVATION_EXPIRE_TASK"
    - path: "src/jobs/runner.ts"
      provides: "crontab populated with `* * * * * reservation.expire` (AM-03 — 1 min minimum)"
      contains: "reservation.expire"
  key_links:
    - from: "src/lib/actions/reservations.ts"
      to: "pg_try_advisory_xact_lock"
      via: "raw SQL inside withTenant tx"
      pattern: "pg_try_advisory_xact_lock\\(hashtext"
    - from: "src/lib/actions/reservations.ts"
      to: "emitOutboxEvent"
      via: "same-tx outbox insert"
      pattern: "emitOutboxEvent\\(db,\\s*'lot\\.reserved'"
    - from: "src/components/eventos/planta-editor.tsx"
      to: "EventSource /api/sse/events/{eventId}/lots"
      via: "useEffect on mount when mode='buyer'"
      pattern: "new EventSource\\("
---

<objective>
Real-time-aware lot reservation slice: a fornecedor clicks an available lot on the planta, the server claims it under an advisory lock, the row + outbox event land atomically, and a scheduled task reclaims any reservation older than 15 minutes. After this plan, two fornecedores racing for the same lot deterministically yield 1 winner and 1 409 — the load-bearing FORN-05 invariant.

Purpose: Close FORN-03 + FORN-04 + FORN-05 + FORN-06 + FORN-13 (atomicity of business-write + outbox event). Plan 02-04 wires the SSE consumer; this plan emits the outbox event + opens the producer side of the channel via emitOutboxEventAndNotify so end-to-end real-time can be tested by 02-04 without rewriting the producer.

Output: reserveLot action with advisory lock, planta-editor buyer mode, scheduled expiry task, atomic outbox emit, 4 test files filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-01-PLAN.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-02-PLAN.md
@src/lib/actions/lot-assignments.ts
@src/lib/actions/safe-action.ts
@src/jobs/raw-sql-from-tenant-db.ts
@src/jobs/enqueue.ts
@src/jobs/runner.ts
@src/jobs/tasks/index.ts
@src/jobs/tasks/zapsign-send-contract.ts
@src/db/with-tenant.ts
@src/db/migrator-pool.ts
@src/components/eventos/planta-editor.tsx
@src/app/[slug]/eventos/[eventId]/planta/page.tsx

<interfaces>
<!-- Patterns from 02-PATTERNS.md -->
<!-- Group D — reservations.ts analog: src/lib/actions/lot-assignments.ts lines 78-171 (guard-INSERT-audit envelope) -->
<!-- §Pattern 1 (lines 332-374): advisory-lock + INSERT reservation + outbox emit -->
<!-- §emitOutboxEvent (lines 846-867): same-tx INSERT into outbox_events using current_setting tenant -->
<!-- §emitOutboxEventAndNotify variant (lines 868-870): adds in-tx pg_notify for SSE-tier latency -->
<!-- §reservation-expire (lines 1022-1046): cross-tenant scan via migratorPool, withTenant per row -->
<!-- §planta-editor extension (lines 321-387): mode='buyer' + onLotClicked + EventSource useEffect -->

From src/lib/actions/lot-assignments.ts (Phase 1):
  - `assignLotToVendorInTenant(db, tenantId, input, userId)` — exact shape to mirror: guard SELECT → INSERT → recordAudit → unique-violation walk-cause-chain (23505 = "Lote já reservado")
  - Partial-unique pattern from Migration 0011 — Phase 2 0017 has the equivalent for lot_reservations

From src/jobs/raw-sql-from-tenant-db.ts:
  - `rawSqlFromTenantDb(db: TenantDb): TransactionSql` — extract postgres.js tx tag for `pg_notify` / `enqueueJob` calls inside withTenant
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: emitOutboxEvent helper + reserveLot Server Action with advisory lock + atomic outbox event</name>
  <files>src/lib/outbox/emit.ts, src/lib/actions/reservations.ts, src/lib/validators/reservations.ts, tests/reservations/create.test.ts, tests/reservations/concurrent.test.ts, tests/outbox/atomic.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 332-374 reserveLot pattern; lines 648-722 reservations.ts analog; lines 840-870 emitOutboxEvent + emitOutboxEventAndNotify; lines 1284-1302 Pattern S1 withTenant; lines 1346-1364 Pattern S4 walk-cause-chain)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (lines 332-374 §Pattern 1; lines 544-548 Pitfall 5 hashtext collision; lines 869 emitOutboxEventAndNotify D-19 same-tx pg_notify)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-05 simplified by AM-01 → 15 min hard; D-16 D-19 AM-03)
    - src/lib/actions/lot-assignments.ts (full file — guard-INSERT-audit envelope)
    - src/lib/actions/safe-action.ts (full file — withTenantAction chain)
    - src/jobs/raw-sql-from-tenant-db.ts (full file)
    - src/db/schema/lot_reservations.ts (full file — created in Plan 02-01)
    - src/db/schema/outbox_events.ts (full file — created in Plan 02-01)
    - src/lib/audit.ts (full file)
  </read_first>
  <behavior>
    - Test 1 (happy path, FORN-04): Approved fornecedor on /{slug}/marketplace/{eventId}/planta clicks an available lot → reserveLot returns { reservation_id, expires_at = now()+15min }; lot_reservations row exists with released_at IS NULL; outbox_events row exists with event_type='lot.reserved', aggregate_id=lot_id, processed_at IS NULL; recordAudit row 'lot_reservation.created' present.
    - Test 2 (FORN-05 concurrent — load-bearing): Spawn 50 concurrent reserveLot calls on the same lot. Assert exactly 1 returns success; remaining 49 return a typed error containing 'Lote já reservado'. lot_reservations has exactly 1 active row. outbox_events has exactly 1 lot.reserved row.
    - Test 3 (FORN-13 atomicity): Force the outbox INSERT to throw mid-tx (e.g., violate a CHECK constraint on event_type). Outer withTenant rolls back; assert lot_reservations is empty AND outbox_events is empty (neither persisted).
    - Test 4 (pending vendor blocked): Vendor with status='pending' attempts reserveLot → typed error 'Fornecedor não aprovado para comprar lotes'. (D-23 — approval gate stays at lot reservation, not at signup.)
    - Test 5 (cross-tenant RLS): Tenant_A vendor attempts to reserve a tenant_B lot via direct DB seed → SELECT lot_rows length 0 → typed error 'Lote não encontrado'. Never sees the cross-tenant lot.
    - Test 6 (TTL boundary): expires_at = inserted_at + 15 min (exact — clock-mocked via vi.setSystemTime).
  </behavior>
  <action>
    Create `src/lib/outbox/emit.ts`:
    - Export `OutboxEventType` union: 'payment.created' | 'payment.paid' | 'payment.failed' | 'lot.reserved' | 'lot.sold' | 'lot.released' | 'lot.status_changed' | 'refund.created'
    - Export `emitOutboxEvent(db: TenantDb, eventType: OutboxEventType, aggregateId: string, payload: Record<string, unknown>): Promise<void>` — INSERT into outbox_events using `current_setting('app.current_tenant_id', true)::uuid` for tenant_id (same pattern as 02-PATTERNS.md lines 846-867).
    - Export `emitOutboxEventAndNotify(db: TenantDb, eventType: 'lot.status_changed', payload: { event_id: string, lot_id: string, new_status: string }): Promise<void>` — combined INSERT + pg_notify('event:${event_id}:lots', JSON.stringify(payload)) inside the same tx via `rawSqlFromTenantDb(db)`. Channel payload ≤ 8000 bytes per Pitfall 3 — send IDs only.

    Create `src/lib/validators/reservations.ts`: Zod `reserveLotSchema` = { eventId: z.string().uuid(), lotId: z.string().uuid(), vendorId: z.string().uuid() }.

    Create `src/lib/actions/reservations.ts` per 02-PATTERNS.md §reservations + §Pattern 1:
    - Pure helper `reserveLotInTenant(db, tenantId, input, userId)`:
      1. Run `SELECT pg_try_advisory_xact_lock(hashtext(${`lot:${eventId}:${lotId}`})::bigint) AS got` via `db.execute(sql\`...\`)`. If got=false → throw 'Lote já reservado por outro fornecedor — atualize a página.'
      2. Re-verify lot status under the lock (defense against TOCTOU): SELECT id FROM lots WHERE id=lotId AND status='available' AND deleted_at IS NULL. If 0 rows → throw 'Lote indisponível.'
      3. Verify vendor is approved: SELECT id FROM vendors WHERE id=vendorId AND status='approved' AND deleted_at IS NULL. If 0 rows → throw 'Fornecedor não aprovado para comprar lotes.'
      4. INSERT INTO lot_reservations { tenant_id, lot_id, vendor_id, event_id, expires_at: sql\`now() + interval '15 minutes'\` } RETURNING *. Wrap in walk-cause-chain catch (Pattern S4) for 23505 → throw 'Lote já reservado'.
      5. `await emitOutboxEvent(db, 'lot.reserved', input.lotId, { reservation_id: rows[0].id, vendor_id, event_id })`.
      6. `await emitOutboxEventAndNotify(db, 'lot.status_changed', { event_id, lot_id, new_status: 'reserved' })` — same-tx pg_notify for SSE-tier latency per AM-03.
      7. `await recordAudit(db, { action: 'lot_reservation.created', entity: 'lot_reservation', entityId: rows[0].id, userId, payload: { lot_id, vendor_id, event_id } })`.
      8. Return { reservation_id: rows[0].id, expires_at: rows[0].expires_at }.
    - Thin action `reserveLot` via `withTenantAction.inputSchema(reserveLotSchema).action(...)` — calls pure helper + revalidatePath.
    - Also export `releaseReservationInTenant(db, tenantId, reservationId)` used by the SAGA cancel handler in Plan 02-06: UPDATE lot_reservations SET released_at=now() WHERE id=? AND released_at IS NULL; emit lot.released outbox event. Audit row 'lot_reservation.released'.

    Fill in `tests/reservations/create.test.ts` (Tests 1, 4, 5, 6), `tests/reservations/concurrent.test.ts` (Test 2 — use Promise.all of 50 `reserveLotInTenant` calls inside parallel `withTenant(tenantId, async (db) => …)` blocks against the same migratorPool — make sure each gets its own postgres.js connection so advisory locks are tx-scoped per call), `tests/outbox/atomic.test.ts` (Test 3 — inject a faulty event_type via direct tx manipulation OR add a temporary debug branch flag inside emitOutboxEvent that throws on a sentinel value, then assert rollback).
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/reservations/create.test.ts tests/reservations/concurrent.test.ts tests/outbox/atomic.test.ts --run</automated>
  </verify>
  <done>All 6 reservation-create tests + 1 concurrent test (50-way race resolves to exactly 1 winner) + 1 atomicity test green. Outbox row + business write rollback together. Vendor approval gate enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlantaEditor mode='buyer' + buyer planta page + reservation.expire scheduled task</name>
  <files>src/components/eventos/planta-editor.tsx, src/app/[slug]/marketplace/[eventId]/planta/page.tsx, src/jobs/tasks/reservation-expire.ts, src/jobs/tasks/index.ts, src/jobs/runner.ts, tests/components/planta-buyer-mode.test.tsx, tests/jobs/reservation-expire.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 321-387 planta-editor extension; lines 1022-1046 reservation-expire task; lines 390-419 runner crontab; lines 425-460 tasks/index.ts registry)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (D-19 D-20 SSE+buyer mode)
    - src/components/eventos/planta-editor.tsx (full file — existing mode='editor' and mode='dashboard')
    - src/app/[slug]/eventos/[eventId]/planta/page.tsx (planta page analog — full file)
    - src/jobs/tasks/zapsign-send-contract.ts (Task<Payload> shape — full file)
    - src/jobs/runner.ts (full file — empty crontab today)
    - src/jobs/tasks/index.ts (full file — registry)
    - src/db/migrator-pool.ts (full file)
  </read_first>
  <behavior>
    - Test 1 (FORN-03 component): PlantaEditor with mode='buyer' + dashboardLots={lot1: status='available', lot2: status='sold', lot3: status='reserved'}: clicking lot1 fires onLotClicked('lot1'); clicking lot2/lot3 does NOT fire onLotClicked. Lot2/lot3 polygon `cursor` resolves to 'not-allowed' (Konva listening prop controls visual state).
    - Test 2 (FORN-06 reservation.expire happy path): seed 3 lot_reservations rows — A expires_at < now() released_at IS NULL, B expires_at < now() released_at IS NOT NULL, C expires_at > now() released_at IS NULL. Run reservationExpire task. Assert A.released_at IS NOT NULL (newly set); B unchanged; C unchanged. Assert outbox_events has 1 'lot.released' row for A's lot.
    - Test 3 (reservation.expire cross-tenant): seed expired reservations in tenant_A and tenant_B. Task processes both — each row's UPDATE happens under withTenant(row.tenant_id) so RLS audit logs land in the correct tenant.
    - Test 4 (reservation.expire batch limit): seed 600 expired rows in tenant_A. Task processes up to LIMIT 500 in one invocation (next cron tick picks up the rest).
  </behavior>
  <action>
    Extend `src/components/eventos/planta-editor.tsx`:
    - Add 'buyer' to `PlantaEditorMode` union (line 60).
    - Add optional prop `onLotClicked?: (lotId: string) => void`.
    - In the existing click handler (lines 601-612), add a `mode === 'buyer'` branch: ONLY fire `onLotClicked(lot.id)` if `dashboardLots[lot.id]?.status === 'available'`. For 'sold' or 'reserved', set `cursor: 'not-allowed'` via Konva's `listening={false}` or shape-level cursor override; do NOT fire callback.
    - In the JSX color-by-status logic (lines 571-589), add buyer-mode palette: available=green (`#10b981` fill 40% opacity), reserved=grey (`#9ca3af`), sold=red (`#ef4444`). Reuse `getLotColorForStatus` from Phase 1 dashboard helper if it accepts a `mode` param; if not, add a sibling helper `getBuyerLotColor(status)`.
    - Add a `useEffect` keyed on `(mode === 'buyer', eventId)` that opens `new EventSource(\`/api/sse/events/${eventId}/lots\`)`. On message (JSON.parse → { lot_id, new_status }), update an internal `buyerLotStatuses` state map and re-render colors. Cleanup `eventSource.close()` on unmount. **The SSE Route Handler ships in Plan 02-04** — this useEffect references the URL; in Plan 02-03 tests, mock EventSource via `vi.stubGlobal('EventSource', ...)`.

    Create `src/app/[slug]/marketplace/[eventId]/planta/page.tsx` Server Component using Pattern S9 boilerplate + analog from src/app/[slug]/eventos/[eventId]/planta/page.tsx:
    - Resolve tenant + activeOrg guard.
    - Load planta metadata + lots-with-status (use existing `getEventLotsForDashboard` from Phase 1 if present — pass it to PlantaEditor as `dashboardLots` map).
    - Mount `<PlantaEditor mode='buyer' eventId={eventId} planta={planta} lots={lots} dashboardLots={lotStatusMap} onLotClicked={...} />`.
    - Render a side panel `<CheckoutSidebar />` stub (real implementation in Plan 02-05 — for now, just a placeholder div showing `Lote {selectedLotId} — checkout em breve`). Client Component holding `selectedLotId` state lifts up via Context or local state.

    Create `src/jobs/tasks/reservation-expire.ts` per 02-PATTERNS.md lines 1022-1046:
    - Export `RESERVATION_EXPIRE_TASK = 'reservation.expire'`.
    - Export `reservationExpire: Task = async (_p, _h) => { ... }` body:
      ```
      const rows = await migratorPool<{id:string,tenant_id:string,lot_id:string,event_id:string}[]>`
        SELECT id, tenant_id, lot_id, event_id FROM lot_reservations
         WHERE expires_at < now() AND released_at IS NULL
         LIMIT 500 FOR UPDATE SKIP LOCKED
      `
      for (const row of rows) {
        await withTenant(row.tenant_id, async (db) => {
          await db.execute(sql`UPDATE lot_reservations SET released_at=now() WHERE id=${row.id} AND released_at IS NULL`)
          await emitOutboxEvent(db, 'lot.released', row.lot_id, { reservation_id: row.id, event_id: row.event_id, reason: 'ttl_expired' })
          await emitOutboxEventAndNotify(db, 'lot.status_changed', { event_id: row.event_id, lot_id: row.lot_id, new_status: 'available' })
        })
      }
      ```
    - Use childLogger({ component: 'reservation.expire' }) for structured logs of the per-batch count.

    Update `src/jobs/tasks/index.ts` to import + register RESERVATION_EXPIRE_TASK in taskList.

    Update `src/jobs/runner.ts` crontab from empty string to `'* * * * * reservation.expire\n'` (AM-03: 1 minute is graphile-worker min; the outbox.drain entry is added in Plan 02-06).

    Fill in `tests/components/planta-buyer-mode.test.tsx` (Test 1 — use @testing-library/react + Konva test stub; verify click does/does not call onLotClicked based on status) and `tests/jobs/reservation-expire.test.ts` (Tests 2, 3, 4 — use migratorPool to seed rows + invoke the Task function directly per tests/test-mocks/graphile-worker.ts harness).
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/components/planta-buyer-mode.test.tsx tests/jobs/reservation-expire.test.ts --run</automated>
  </verify>
  <done>buyer-mode + reservation-expire tests green. Crontab now lists reservation.expire. PlantaEditor renders buyer-mode with click-filter; sold/reserved cursor blocked; SSE useEffect mounted (URL only — handler in Plan 02-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reserveLot Server Action | Untrusted eventId/lotId — Zod validate; RLS enforces tenant scoping |
| migratorPool ↔ reservation.expire | Cross-tenant scan via BYPASSRLS-equivalent role; UPDATE must re-enter withTenant per row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering (TOCTOU) | Two clients clicking same lot | mitigate | pg_try_advisory_xact_lock + re-SELECT status under lock + partial-unique index — three layers of defense (RESEARCH §Anti-patterns) |
| T-02-03-02 | DoS | Vendor spam-reserves all lots without paying | accept | TTL 15 min auto-releases; FORN-15 waitlist gives others another chance; Phase 3 may add per-vendor concurrent-reservation cap |
| T-02-03-03 | Spoofing | Client sends spoofed lotId from a different event | mitigate | RLS isolates tenant; in-action re-SELECT verifies lot.event_id matches input.eventId |
| T-02-03-04 | Tampering | Hashtext collision on (event_id, lot_id) | accept | Pitfall 5 — acceptable at Trindade piloto scale (~5000 lots); contract test warns at >10M lots; Phase 4 may switch to (int4,int4) two-key form |
| T-02-03-05 | Information disclosure | SSE URL pre-fetched by buyer-mode useEffect leaks tenant/event mapping | mitigate | SSE handler (Plan 02-04) verifies session.activeOrganizationId === tenant.id before opening stream; this plan only emits the URL — handler validates |
| T-02-03-06 | DoS | Stuck reservation.expire task (poison row) | mitigate | FOR UPDATE SKIP LOCKED + processing_status='failed' fallback (Pitfall 11) |
| T-02-03-SC | n/a | No new npm packages | n/a | |
</threat_model>

<verification>
- `pnpm vitest tests/reservations/ tests/jobs/reservation-expire.test.ts tests/outbox/atomic.test.ts tests/components/planta-buyer-mode.test.tsx --run` green
- `pnpm test --run` total ≥ 200 passing (181 + 9 from 02-02 + ≥10 from this plan)
- `pnpm tsc --noEmit` 0
- `pnpm lint` 0
- Manual: visit /trindade/marketplace/{eventId}/planta — see colored planta; click available lot — sidebar opens with stub message
</verification>

<success_criteria>
- FORN-03 + FORN-04 + FORN-05 + FORN-06 + FORN-13 all green
- 50-way race resolves to exactly 1 winner (load-bearing FORN-05)
- reservation.expire crontab live; expired reservations auto-released
- Outbox row + business write atomicity proven via rollback test
</success_criteria>

<output>
Create `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-03-SUMMARY.md` when done.
</output>
