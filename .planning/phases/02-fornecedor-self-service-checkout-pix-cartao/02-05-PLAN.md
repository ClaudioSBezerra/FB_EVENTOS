---
phase: 02-fornecedor-self-service-checkout-pix-cartao
plan: 05
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-03
files_modified:
  - src/lib/actions/cart.ts
  - src/lib/validators/cart.ts
  - src/lib/pagarme/types.ts
  - src/lib/pagarme/client.ts
  - src/lib/pagarme/hmac.ts
  - src/lib/actions/checkout.ts
  - src/lib/validators/checkout.ts
  - src/app/api/webhooks/pagarme/route.ts
  - src/jobs/tasks/payment-process-webhook.ts
  - src/jobs/tasks/index.ts
  - src/components/checkout/checkout-sidebar.tsx
  - src/components/checkout/installments-table.tsx
  - src/app/[slug]/checkout/[cartId]/page.tsx
  - src/lib/env.ts
  - .env.example
  - .env.production.example
  - docs/adr/0005-webhook-hmac-strategy.md
  - tests/cart/total.test.ts
  - tests/payments/checkout-paths.test.ts
  - tests/webhooks/hmac-verify.test.ts
  - tests/webhooks/pagarme-idempotent.test.ts
  - tests/webhooks/perf.test.ts
  - tests/probes/pagarme-hmac-header-probe.test.ts
  - tests/probes/pagarme-installments-shape-probe.test.ts
  - src/lib/pagarme/installments-shape.generated.ts
autonomous: false
requirements:
  - FORN-08
  - FORN-09
  - FORN-10
  - FORN-11
  - FORN-12
must_haves:
  truths:
    - "Cart total = lot price (computeLotPrice from Phase 1) + sum(cart_addon_lines.price_brl_cents_snapshot) — pure helper testable without DB"
    - "Checkout PIX path: POST to Pagar.me sandbox returns qr_code + qr_code_url synchronously; payments row inserted; pagarme_orders row inserted; outbox 'payment.created' emitted in same tx"
    - "Checkout credit_card path: installments 1..12 accepted; installments shape (interest_type, response echo of per-installment amount) probe-verified at execute time per AM-06 — same Task 1 CHECKPOINT pins both AM-02 HMAC and AM-06 installments via a single sandbox round-trip"
    - "Boleto is NOT a selectable method (AM-01) — UI shows 2 tiles (PIX + Cartão); payment_method enum has no 'boleto' value in Phase 2"
    - "HMAC header name + algorithm + encoding (base64 vs hex) probe-verified at execute time per AM-02 — Task 1 is a CHECKPOINT that runs before the handler refactor; probe outcome pinned into hmac.ts as a code comment + ADR-0005"
    - "installments-shape probe-verify (AM-06): same Task 1 sandbox round-trip creates a 6x R$ 1200 credit_card charge and inspects the response — captures whether Pagar.me echoes per-installment amounts in `installments_table` (or similar key), and what `interest_type` actually maps to. Outcome pinned into `src/lib/pagarme/installments-shape.generated.ts`. Checkout UI consumes this constant"
    - "Webhook handler: HMAC verify FIRST (reading raw bytes via req.arrayBuffer per Pitfall 1) → INSERT into payment_webhooks_inbox with PK on gateway_event_id ON CONFLICT DO NOTHING (idempotent) → enqueue payment.process-webhook → respond 200 in <100ms p95"
    - "payment.process-webhook task inherits Phase 1 belt-and-suspenders re-fetch defense (pagarmeClient.getOrder + trust API status) + FSM transitions + idempotency by dropping into-terminal-state writes; emits outbox 'payment.paid' or 'payment.failed' instead of inline email enqueue"
    - "Duplicate webhook delivery: 1 inbox row + 0 extra side effects (PK conflict suppresses the second INSERT)"
  artifacts:
    - path: "src/lib/actions/cart.ts"
      provides: "addAddonToCart, removeAddonFromCart, computeCartTotalInTenant pure helpers"
      contains: "computeCartTotal"
    - path: "src/lib/pagarme/hmac.ts"
      provides: "verifyWebhookSignature(rawBody, signatureHeader, secret) per RESEARCH §Pattern 4"
      contains: "timingSafeEqual"
    - path: "src/lib/actions/checkout.ts"
      provides: "checkoutCartInTenant pure helper + checkoutCart Server Action"
      contains: "X-Idempotency-Key"
    - path: "src/app/api/webhooks/pagarme/route.ts"
      provides: "HMAC + inbox + enqueue refactor; <100ms p95"
      contains: "verifyWebhookSignature"
      contains_2: "ON CONFLICT (gateway_event_id) DO NOTHING"
    - path: "src/jobs/tasks/payment-process-webhook.ts"
      provides: "Worker handler that does re-fetch + FSM + outbox emit; withTenant(payload.tenant_id) per Pitfall 8"
      contains: "PAYMENT_PROCESS_WEBHOOK_TASK"
    - path: "src/components/checkout/checkout-sidebar.tsx"
      provides: "Cart + add-on checkboxes + 2 method tiles (PIX / Cartão) + InstallmentsTable on Cartão"
    - path: "docs/adr/0005-webhook-hmac-strategy.md"
      provides: "ADR pinning probe-verified header name + ME-platform context + belt-and-suspenders justification"
  key_links:
    - from: "src/app/api/webhooks/pagarme/route.ts"
      to: "src/lib/pagarme/hmac.ts::verifyWebhookSignature"
      via: "raw arrayBuffer pre-parse"
      pattern: "verifyWebhookSignature\\(rawBody"
    - from: "src/app/api/webhooks/pagarme/route.ts"
      to: "payment_webhooks_inbox"
      via: "ON CONFLICT DO NOTHING INSERT via migratorPool"
      pattern: "ON CONFLICT \\(gateway_event_id\\) DO NOTHING"
    - from: "src/app/api/webhooks/pagarme/route.ts"
      to: "payment.process-webhook task"
      via: "enqueueJob with {inbox_id, tenant_id}"
      pattern: "'payment\\.process-webhook'"
---

<objective>
End-to-end checkout slice — the highest-stakes plan of Phase 2. A fornecedor with a 15-min reservation picks PIX or Cartão (parcelado), the Server Action mints a Pagar.me order, the response is rendered (QR / installments table), Pagar.me sends a webhook back, the handler HMAC-verifies + inboxes + enqueues, and the worker re-fetches + emits the outbox event Plan 02-06 consumes. After this plan, a real Trindade fornecedor can pay PIX end-to-end in Pagar.me sandbox.

Purpose: Close FORN-08, FORN-09, FORN-10, FORN-11, FORN-12. The HMAC probe-test (AM-02) gates the handler refactor — operator confirms the actual header before code lands.

Output: Cart action + checkout action + HMAC helper + webhook refactor + process-webhook worker + checkout UI + ADR-0005 + 6 test files filled + 1 probe-test file.

**autonomous: false** — Task 1 is a `checkpoint:human-verify gate="blocking"` for the HMAC probe (operator must hit Pagar.me sandbox dashboard and confirm the header name before downstream code is written).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md
@.planning/phases/01-organizadora-end-to-end-piloto-festa-de-trindade/01-06-SUMMARY.md
@src/lib/pagarme/client.ts
@src/lib/pagarme/types.ts
@src/lib/actions/payments.ts
@src/lib/validators/payment.ts
@src/app/api/webhooks/pagarme/route.ts
@src/lib/env.ts
@src/lib/audit.ts
@src/jobs/raw-sql-from-tenant-db.ts
@src/jobs/enqueue.ts
@src/jobs/tasks/zapsign-send-contract.ts
@src/components/contracts/create-charge-button.tsx
@src/components/payments/pix-qr.tsx
@src/lib/lots/price.ts
@docs/adr/0002-e-sign-provider.md

<interfaces>
<!-- Patterns from 02-PATTERNS.md -->
<!-- §pagarme/client.ts extend (lines 108-161): add cancelCharge (DELETE — AM-04) + installments in credit_card payload; DROP boleto path -->
<!-- §pagarme/hmac.ts (lines 809-836): RESEARCH §Pattern 4 verbatim — header name + encoding probe-pinned -->
<!-- §webhooks/pagarme/route.ts refactor (lines 164-226): HMAC + inbox + enqueue + <100ms -->
<!-- §payments.ts createCharge → checkout.ts split (lines 229-318): cart aggregation + outbox emit -->
<!-- §payment-process-webhook task (lines 990-1018): inherits Phase 1 FSM but emits outbox 'payment.paid|failed' instead of inline email -->
<!-- §Pattern S10 (lines 1465-1476): probe-test for external-system contract — REQUIRED for AM-02 -->

From src/lib/pagarme/client.ts (Phase 1):
  - `getPagarmeBaseUrl()` reads PAGARME_ENV → sandbox vs production
  - `buildAuthHeader()` uses Basic Auth with TRAILING colon
  - `createOrder(payload)` POSTs to /core/v5/orders with X-Idempotency-Key
  - `getOrder(orderId)` GETs for re-fetch defense

From src/app/api/webhooks/pagarme/route.ts (Phase 1):
  - Lines 114-130: entry + Basic Auth verify
  - Lines 78-90: resolveTenantForOrderId via migratorPool + sysreader bounded function
  - Lines 161-172: belt-and-suspenders re-fetch (PRESERVE)
  - Lines 175-272: FSM + idempotency + email enqueue (move into worker handler)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] HMAC probe-test + installments-shape probe — single sandbox round-trip pinning AM-02 + AM-06</name>
  <files>tests/probes/pagarme-hmac-header-probe.test.ts, tests/probes/pagarme-installments-shape-probe.test.ts, src/lib/pagarme/installments-shape.generated.ts, docs/adr/0005-webhook-hmac-strategy.md</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (lines 767-770 Open Q1; lines 894-906 §Known Threat Patterns webhook spoofing)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (AM-02 supersede D-13)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 1465-1476 Pattern S10 probe-test)
    - .planning/phases/00-foundation-stack-lock-anti-pitfall-hardening/00-06-PLAN.md (Phase 0 Plan 06 — add_job signature probe pattern; if file path differs, locate via `find .planning/phases/00-* -name '*probe*.md' 2>/dev/null` and read closest match)
    - tests/jobs/add-job-signature-probe.test.ts (if exists — the canonical probe-test from Phase 0)
  </read_first>
  <what-built>
    Operator-driven probe of the actual Pagar.me v5 webhook HMAC contract on the sandbox account.

    Sub-step 1 (operator action — out-of-code): Log into Pagar.me dashboard → Settings → Webhooks → pick the existing FB_EVENTOS sandbox webhook (configured in Phase 1) → look for the HMAC/signature signing-key configuration UI. Note:
    - Header name (e.g. `X-Hub-Signature` vs `X-ME-WEBHOOK-SIGNATURE` vs `Webhook-Signature`)
    - Algorithm + encoding (HMAC-SHA256 base64 vs hex)
    - Secret-rotation UI (where can the secret be rotated; does it support multiple active secrets for zero-downtime rotation)

    Sub-step 2 (operator action — set env): copy the signing secret into `PAGARME_WEBHOOK_SIGNING_SECRET=<value>` in local `.env.local` (gitignored). Add the same key + a placeholder to `.env.example` and `.env.production.example` (committed, no real value).

    Sub-step 3 (code task): Create `tests/probes/pagarme-hmac-header-probe.test.ts`. Vitest case that:
    a. Reads the operator-pinned header name + encoding from `tests/probes/pagarme-hmac-header-probe.config.json` (gitignored — operator fills in).
    b. Pings the Pagar.me sandbox webhook by triggering a sandbox event (use existing simulatePagarmeWebhook helper from tests/e2e/fixtures/d14-gate-fixtures.ts).
    c. Inspects the actual headers received at a local /probe endpoint (set the dashboard webhook URL to the probe endpoint for the duration of the test).
    d. Asserts: configured header name matches received name; configured encoding matches received encoding; signature verifies against the secret (via `verifyWebhookSignature` candidate body — using base64 first, fall back to hex if it doesn't match).
    e. Outputs (on success) a code snippet to paste into hmac.ts as the LOAD-BEARING comment block + ADR-0005 §Decision.

    Sub-step 4 (code task): Author `docs/adr/0005-webhook-hmac-strategy.md` following docs/adr/0002-e-sign-provider.md shape (Context → Decision → Comparison table → Consequences → Escape-hatch → References). Pin the probe outcome. Reference belt-and-suspenders re-fetch from Phase 1 D-13.

    **If the operator account does not yet have HMAC enabled (Phase 1 was Basic Auth only):** flag this to the user, fall back to either (a) keeping Basic Auth in production while turning ON HMAC for sandbox+staging (Phase 1 stays unchanged in prod), or (b) upgrading the dashboard config + flipping all envs at once. Surface the choice in the CHECKPOINT message.

    Sub-step 5 (code task — AM-06 installments shape probe): Create `tests/probes/pagarme-installments-shape-probe.test.ts`. Vitest case that:
    a. Uses the existing Pagar.me sandbox API key (`PAGARME_SANDBOX_API_KEY` env, already provisioned in Phase 1)
    b. Creates a sandbox credit_card charge with amount=120000 (R$ 1200), installments=6, payment_method=credit_card, and an explicit `interest_type` field if the docs example uses one (try both `'compound'` and absent to detect default)
    c. Inspects the full response JSON: captures (i) whether an `installments_table` / `installment_plan` / similar field exists with per-installment amounts; (ii) what the response says about effective `interest_type`; (iii) total charged vs base amount (computes effective APR)
    d. Outputs a generated TypeScript file `src/lib/pagarme/installments-shape.generated.ts` exporting:
       - `PAGARME_INSTALLMENTS_REQUEST_FIELDS: { installments: number; interest_type?: 'compound' | 'simple' }`
       - `PAGARME_INSTALLMENTS_RESPONSE_KEY: 'installments_table' | 'installment_plan' | null` (null = compute juros client-side)
       - A code comment block with the probe date + verbatim sample response excerpt
    e. ADR-0005 is extended (single ADR) with a §"Installments shape (AM-06)" section pinning the probe outcome alongside the HMAC section.

    The two probes share one sandbox round-trip session so operator only logs into the Pagar.me dashboard once.
  </what-built>
  <how-to-verify>
    Operator (human):
    1. Reads the screenshot or dashboard text the executor printed; confirms header name + encoding match what the executor wrote into hmac.ts + ADR-0005 (verbatim).
    2. Confirms `PAGARME_WEBHOOK_SIGNING_SECRET` is in `.env.local` and NOT in any committed file (run `git diff --cached -- '.env*'` and inspect).
    3. Runs `pnpm vitest tests/probes/pagarme-hmac-header-probe.test.ts tests/probes/pagarme-installments-shape-probe.test.ts --run` → exits 0 (both probes).
    4. Inspects the generated `src/lib/pagarme/installments-shape.generated.ts` — verifies the recorded probe outcome looks plausible (e.g., `PAGARME_INSTALLMENTS_RESPONSE_KEY` is one of the expected values and the sample APR matches Brazilian-market norms for the chosen interest_type).

    If either probe exits 0 BUT the operator observes a mismatch between the sandbox actual behavior and what the test asserts, that is a probe-test bug; abort and escalate. Do NOT silently lower assertions to match observed reality without operator confirmation.
  </how-to-verify>
  <resume-signal>Type "approved 02-05 probes — hmac_header=$NAME hmac_encoding=$ENCODING installments_key=$RESP_KEY interest_type=$IT" with the four values the operator observed.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cart + Pagar.me client extensions (installments, cancelCharge) + HMAC helper</name>
  <files>src/lib/actions/cart.ts, src/lib/validators/cart.ts, src/lib/pagarme/types.ts, src/lib/pagarme/client.ts, src/lib/pagarme/hmac.ts, src/lib/env.ts, .env.example, .env.production.example, tests/cart/total.test.ts, tests/webhooks/hmac-verify.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 108-161 pagarme/client extensions; lines 809-836 hmac.ts; lines 320-322 boleto path DELETE per AM-01; lines 156-160 installments)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-01 D-02 D-03 AM-01 AM-04)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (lines 700+ Open Q3 — installments echo verified at sandbox)
    - src/lib/pagarme/types.ts (full file)
    - src/lib/pagarme/client.ts (full file)
    - src/lib/validators/payment.ts (full file)
    - src/lib/lots/price.ts (full file)
    - src/lib/env.ts (full file)
    - .env.example (full file)
  </read_first>
  <behavior>
    - Test 1 (FORN-08 cart math): Lot priced R$ 1.000 + 2 add-ons (R$ 200, R$ 80) → computeCartTotal returns R$ 1.280 (centavos 128000).
    - Test 2 (cart add-on snapshot): When addAddonToCart is called, snapshot the addon.price_brl_cents into cart_addon_lines.price_brl_cents_snapshot (so subsequent price changes on event_addons don't affect already-committed cart math).
    - Test 3 (cart add-on max_qty): addAddonToCart honors event_addons.max_qty — exceeding throws.
    - Test 4 (HMAC verify happy — base64 OR hex per probe outcome): verifyWebhookSignature accepts a valid signature; rejects an invalid one.
    - Test 5 (HMAC tampered body): Change one byte → verify returns false (timingSafeEqual catches it).
    - Test 6 (HMAC empty header): verifyWebhookSignature returns false (not throw).
    - Test 7 (Pagar.me cancelCharge DELETE happy path — MSW mock): cancelCharge(chargeId) hits DELETE /core/v5/charges/{id}; with { amount: 12300 } body for partial refund (AM-04).
    - Test 8 (installments echo): createOrder with credit_card + installments=12 — Pagar.me MSW returns installment_amount in response; types.ts shape includes it.
  </behavior>
  <action>
    Extend `src/lib/pagarme/types.ts`:
    - Add `pagarmeRefundResponseSchema` matching Pagar.me v5 DELETE /charges/:id response.
    - Extend `pagarmeWebhookEventSchema` with new event subtypes: `charge.refunded`, `charge.partial_canceled`, `order.canceled`, `order.payment_failed` (per RESEARCH webhook events list).
    - Extend `pagarmeOrderCreateRequest.payments[].credit_card` schema with `installments: z.number().int().min(1).max(12).optional()` + `statement_descriptor: z.string().optional()`.
    - REMOVE any boleto schema branches (AM-01 — boleto deferred). If types.ts had `payment_method: z.enum(['pix','credit_card','boleto'])` change to `z.enum(['pix','credit_card'])`. Same for validators/payment.ts.

    Extend `src/lib/pagarme/client.ts`:
    - Add `cancelCharge(chargeId: string, opts?: { amount?: number }): Promise<PagarmeRefundResponse>` doing `fetch(\`${getPagarmeBaseUrl()}/charges/${chargeId}\`, { method: 'DELETE', headers: {Authorization: buildAuthHeader(), 'Content-Type': 'application/json', Accept: 'application/json'}, body: opts?.amount ? JSON.stringify({amount: opts.amount}) : undefined })` then `pagarmeRefundResponseSchema.parse(json)`.
    - Mirror `getOrder` (lines 116-130) for shape — DO NOT add a `refundCharge` separate function; AM-04 documents one endpoint for both cancel and refund.

    Create `src/lib/pagarme/hmac.ts` per RESEARCH §Pattern 4 + probe outcome from Task 1:
    - Export `verifyWebhookSignature(rawBody: Buffer, signatureHeader: string | null, secret: string): boolean`.
    - Body uses createHmac('sha256', secret).update(rawBody).digest() + timingSafeEqual.
    - The `Buffer.from(signatureHeader, 'base64')` decoding is hard-coded if probe confirmed base64; OR `Buffer.from(signatureHeader, 'hex')` if probe confirmed hex. PIN the choice with a LOAD-BEARING comment block referencing tests/probes/pagarme-hmac-header-probe.test.ts.
    - Export const `PAGARME_HMAC_HEADER_NAME = '<probe-pinned value>'` (so the Route Handler reads `req.headers.get(PAGARME_HMAC_HEADER_NAME.toLowerCase())`).

    Update `src/lib/env.ts` adding `PAGARME_WEBHOOK_SIGNING_SECRET: z.string().min(16)` to the env schema (alongside existing PAGARME_* vars). Update `.env.example` + `.env.production.example` adding `PAGARME_WEBHOOK_SIGNING_SECRET=<set in Pagar.me dashboard; gitignored>` placeholder.

    Create `src/lib/validators/cart.ts`: Zod schemas for `addAddonSchema = { reservationId, addonId, quantity (default 1) }`, `removeAddonSchema = { reservationId, addonLineId }`.

    Create `src/lib/actions/cart.ts`:
    - Pure helpers `addAddonToCartInTenant(db, tenantId, input, userId)`: verify reservation belongs to vendor + not expired; SELECT addon for max_qty + active=true; INSERT cart_addon_lines with `price_brl_cents_snapshot = addon.price_brl_cents` (snapshot decision per Test 2 behavior).
    - `removeAddonFromCartInTenant(db, tenantId, input)`: DELETE from cart_addon_lines WHERE id=? AND reservation belongs to vendor.
    - `computeCartTotalInTenant(db, tenantId, reservationId): Promise<{ lot_brl_cents, addons_brl_cents, total_brl_cents }>`. Lot price via existing Phase 1 `computeLotPrice` (src/lib/lots/price.ts). Add-ons sum via SELECT SUM(price_brl_cents_snapshot * quantity) FROM cart_addon_lines WHERE reservation_id=?.
    - Thin actions `addAddonToCart` and `removeAddonFromCart` via `withTenantAction`.

    Fill in `tests/cart/total.test.ts` (Tests 1-3) and `tests/webhooks/hmac-verify.test.ts` (Tests 4-6).
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/cart/total.test.ts tests/webhooks/hmac-verify.test.ts --run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>Cart helpers + Pagar.me extensions + HMAC helper land; 8 tests green; .env files updated with PAGARME_WEBHOOK_SIGNING_SECRET placeholder; no boleto branches remain anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: checkout.ts + webhook handler refactor + payment.process-webhook worker + checkout UI</name>
  <files>src/lib/actions/checkout.ts, src/lib/validators/checkout.ts, src/app/api/webhooks/pagarme/route.ts, src/jobs/tasks/payment-process-webhook.ts, src/jobs/tasks/index.ts, src/components/checkout/checkout-sidebar.tsx, src/components/checkout/installments-table.tsx, src/app/[slug]/checkout/[cartId]/page.tsx, tests/payments/checkout-paths.test.ts, tests/webhooks/pagarme-idempotent.test.ts, tests/webhooks/perf.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 229-318 checkout.ts split; lines 164-226 webhook refactor; lines 990-1018 payment-process-webhook; lines 1236-1245 checkout-sidebar)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-13 D-14 D-15 AM-02; D-02 D-03 AM-01)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (lines 601-644 webhook handler skeleton; lines 519-530 Pitfall 1 raw bytes; lines 526-530 Pitfall 2 retry storm)
    - src/lib/actions/payments.ts (full file — createChargeInTenant pattern)
    - src/app/api/webhooks/pagarme/route.ts (full file — REFACTOR target)
    - src/jobs/tasks/zapsign-send-contract.ts (Task pattern — full file)
    - src/components/contracts/create-charge-button.tsx (full file — dialog + method picker analog)
    - src/components/payments/pix-qr.tsx (full file — reused for PIX response)
    - src/jobs/raw-sql-from-tenant-db.ts (full file)
  </read_first>
  <behavior>
    - Test 1 (PIX happy path — FORN-09): Fornecedor with active reservation + 0 addons calls checkoutCart(reservationId, method='pix') → Server Action POSTs to Pagar.me MSW → response includes qr_code + qr_code_url → payments row inserted with status='pending' + gateway_order_id + last_transaction.pix_qr_code; pagarme_orders row inserted; outbox 'payment.created' emitted in same tx (assert via SELECT outbox_events WHERE event_type='payment.created').
    - Test 2 (Credit card with installments=12): checkoutCart(reservationId, method='credit_card', installments=12, card_token='tok_x') → Pagar.me MSW returns installment_amount echo; payments row stores installments=12; returns to client with status='pending' + redirect/checkout token if needed.
    - Test 3 (Cart + addons): Reservation with 2 add-ons → checkout total reflects sum; Pagar.me payload items[] has 1+2 lines (lot + 2 addons).
    - Test 4 (boleto rejected at validator): checkoutCart with method='boleto' → Zod-level rejection (boleto not in enum per AM-01).
    - Test 5 (FORN-10 idempotency): Webhook delivered twice with same gateway_event_id → only 1 payment_webhooks_inbox row; payment.process-webhook task enqueued once (or twice — but the worker's terminal-state idempotency means only 1 FSM transition; assert this).
    - Test 6 (FORN-11 invalid HMAC): POST webhook with mangled signature → 401; no inbox row created.
    - Test 7 (FORN-12 <100ms p95 perf): Send 50 webhook POSTs (HMAC valid, varied gateway_event_id); measure handler duration; p95 ≤ 100ms. Use vi.spyOn + node:perf_hooks. The handler does only: read body → HMAC verify → migratorPool tenant resolve → migratorPool INSERT inbox ON CONFLICT DO NOTHING → enqueueJob → respond 200.
    - Test 8 (re-fetch defense preserved): Worker handler payment.process-webhook calls pagarmeClient.getOrder(orderId) and uses THAT status, not payload status. If payload says paid but API says failed → payment.status='failed' (Phase 1 D-13 belt-and-suspenders test in tests/payments/pagarme-webhook.test.ts case 4 — preserve).
    - Test 9 (outbox emit in worker): When the FSM transitions to 'paid', worker emits outbox 'payment.paid' (NOT inline email enqueue). When 'failed', emits 'payment.failed'. The handlers in Plan 02-06 do the side effects.
  </behavior>
  <action>
    Create `src/lib/validators/checkout.ts`: Zod `checkoutCartSchema = { reservationId: uuid, method: z.enum(['pix','credit_card']), card_token: z.string().optional(), installments: z.number().int().min(1).max(12).optional() }`. Refine: method='credit_card' requires card_token; installments ≥ 1.

    Create `src/lib/actions/checkout.ts` per 02-PATTERNS.md lines 729-735 + Phase 1 createChargeInTenant pattern:
    - Pure helper `checkoutCartInTenant(db, tenantId, input, userId)`:
      1. SELECT lot_reservations WHERE id=input.reservationId + ensure vendor=ctx.vendor + expires_at > now() + released_at IS NULL. Reject otherwise.
      2. Compute totals via `computeCartTotalInTenant(db, tenantId, reservationId)`.
      3. Build Pagar.me items[] = [lot, ...addons] (each item has amount in centavos + description).
      4. Mint idempotency key: `checkout-${reservationId}-${randomBytes(8).toString('hex')}` — reuse Phase 1 `generateIdempotencyKey` shape.
      5. INSERT payments + pagarme_orders rows (mirror Phase 1 createChargeInTenant lines 144-343).
      6. POST to Pagar.me via pagarmeClient.createOrder({ ...payload, payments: [{ payment_method: input.method, pix?: { expires_in: 900 } /* 15 min — matches reservation TTL */, credit_card?: { installments: input.installments, card_token } }] }).
      7. UPDATE payments + pagarme_orders with response (gateway_order_id, gateway_charge_id, qr_code, qr_code_url, installment_amount, status='pending').
      8. **emit `outbox 'payment.created'`** in same tx with payload { payment_id, reservation_id, vendor_id, lot_id, addons, total_brl_cents }.
      9. recordAudit('payment.created'). Catch Pagar.me errors → recordAuditOutOfBand pattern from Phase 1.
      10. Return { payment_id, status: 'pending', pix?: { qr_code, qr_code_url } | credit_card?: { installment_amount, installments } }.
    - Thin action `checkoutCart` via `withTenantAction`.

    Refactor `src/app/api/webhooks/pagarme/route.ts` per 02-PATTERNS.md lines 164-226 + RESEARCH lines 601-644:
    - REMOVE `verifyBasicAuth` call.
    - Add `import { verifyWebhookSignature, PAGARME_HMAC_HEADER_NAME } from '@/lib/pagarme/hmac'`.
    - Body skeleton:
      ```
      const rawBody = Buffer.from(await req.arrayBuffer())   // Pitfall 1: raw bytes before parse
      const sig = req.headers.get(PAGARME_HMAC_HEADER_NAME.toLowerCase())
      const secret = process.env.PAGARME_WEBHOOK_SIGNING_SECRET
      if (!secret) → 500 misconfigured (log error)
      if (!verifyWebhookSignature(rawBody, sig, secret)) → 401
      let event = pagarmeWebhookEventSchema.parse(JSON.parse(rawBody.toString('utf8')))
      // Resolve tenant denorm per Open Q4
      const tenantInfo = await resolveTenantForOrderId(event.data.id_or_order_id)
      if (!tenantInfo) → 200 ignored (graceful: order not in our DB)
      // INSERT inbox via migratorPool (BYPASSRLS for cross-tenant write)
      await migratorPool`
        INSERT INTO payment_webhooks_inbox (gateway_event_id, tenant_id, event_type, payload, received_at)
        VALUES (${event.id}, ${tenantInfo.tenantId}, ${event.type}, ${rawBody.toString('utf8')}::jsonb, now())
        ON CONFLICT (gateway_event_id) DO NOTHING
      `
      await enqueueJob(migratorPool, 'payment.process-webhook', { inbox_id: event.id, tenant_id: tenantInfo.tenantId })
      return NextResponse.json({ ok: true }, { status: 200 })
      ```
    - Performance assertion (FORN-12) drives the hot-path tightness — the handler does NO `withTenant`, NO Pagar.me re-fetch, NO email enqueue. All that moves into the worker.
    - PRESERVE existing `resolveTenantForOrderId` migratorPool sysreader bounded-function call (Phase 1 lines 78-90 — function fb_lookup_tenant_for_pagarme_order).

    Create `src/jobs/tasks/payment-process-webhook.ts` per 02-PATTERNS.md lines 990-1018 + Phase 1 webhook handler logic lines 175-272:
    - Export `PAYMENT_PROCESS_WEBHOOK_TASK = 'payment.process-webhook'`.
    - `paymentProcessWebhook: Task = async (rawPayload, helpers)` with Zod parsed `{ inbox_id, tenant_id }`.
    - Body:
      ```
      await withTenant(payload.tenant_id, async (db) => {
        const inboxRow = (await migratorPool` SELECT * FROM payment_webhooks_inbox WHERE gateway_event_id = ${payload.inbox_id} `)[0]
        if (!inboxRow || inboxRow.processing_status !== 'pending') return
        const event = JSON.parse(inboxRow.payload)
        const orderId = event.data.id ?? event.data.order_id
        // belt-and-suspenders re-fetch (Phase 1 D-13 PRESERVED)
        const apiOrder = await pagarmeClient.getOrder(orderId)
        // FSM transition + terminal-state idempotency (Phase 1 lines 175-272 — moved verbatim)
        const newStatus = decideNewStatus(apiOrder)  // existing helper from Phase 1 payments.ts (or move into checkout.ts utilities)
        const paymentRow = await db.select().from(payments).where(eq(payments.gatewayOrderId, orderId)).limit(1).then(r=>r[0])
        if (paymentRow.status === newStatus || isTerminal(paymentRow.status)) {
          // Idempotent no-op: drop into-terminal-state writes
          await migratorPool` UPDATE payment_webhooks_inbox SET processing_status='processed', processed_at=now() WHERE gateway_event_id=${payload.inbox_id} `
          return
        }
        await db.update(payments).set({ status: newStatus, paidAt: newStatus === 'paid' ? sql`now()` : null }).where(eq(payments.id, paymentRow.id))
        // emit outbox event for downstream handlers (Plan 02-06)
        await emitOutboxEvent(db, newStatus === 'paid' ? 'payment.paid' : newStatus === 'failed' ? 'payment.failed' : 'payment.created', paymentRow.id, { contract_id: paymentRow.contract_id, vendor_id: paymentRow.vendor_id, lot_id: paymentRow.lot_id, order_id: orderId, status: newStatus })
        await recordAudit(db, { action: `payment.${newStatus}`, entity: 'payment', entityId: paymentRow.id, userId: SYSTEM_USER_ID, payload: { order_id: orderId } })
      })
      // Mark inbox processed via migratorPool (NOT inside withTenant — inbox row is cross-tenant scan-friendly)
      await migratorPool` UPDATE payment_webhooks_inbox SET processing_status='processed', processed_at=now() WHERE gateway_event_id=${payload.inbox_id} `
      ```
    - Wrap top-level errors with status='failed' + audit-out-of-band.

    Update `src/jobs/tasks/index.ts` registering PAYMENT_PROCESS_WEBHOOK_TASK.

    Create `src/components/checkout/checkout-sidebar.tsx` per 02-PATTERNS.md lines 1236-1245:
    - Reuse the shape of src/components/contracts/create-charge-button.tsx (dialog + method tiles).
    - Show cart line items (lot + add-on checkboxes — toggling calls addAddonToCart / removeAddonFromCart).
    - Show 2 method tiles: PIX (icon + "Pagar agora"), Cartão (icon + "Parcelar em até 12x").
    - On PIX selected + "Pagar" pressed: call `checkoutCart` action; on response render `<PixQR>` (Phase 1 component).
    - On Cartão selected: render `<InstallmentsTable>` showing 1..12 with calculated installment_amount from Pagar.me (call a separate Server Action `previewInstallments(reservationId): Promise<Array<{n, amount}>>` that POSTs to Pagar.me /core/v5/orders/preview if available, or computes locally with a static juros rate fallback per the planner-discretion note in CONTEXT.md). For simplicity: compute locally with a default 3.5% rate; Q3 sandbox-verification refines.

    Create `src/components/checkout/installments-table.tsx`: Simple table 1..12 with row { installment, valor unit, juros %, total }. Single-select via radio.

    Create `src/app/[slug]/checkout/[cartId]/page.tsx` Server Component — load reservation + cart lines + mount `<CheckoutSidebar>` Client Component.

    Fill in `tests/payments/checkout-paths.test.ts` (Tests 1-4 — use MSW Pagar.me handlers from Plan 02-01), `tests/webhooks/pagarme-idempotent.test.ts` (Test 5), `tests/webhooks/perf.test.ts` (Test 7 — measure with perf_hooks; assert p95 ≤ 100ms over 50 calls). Test 6 already in hmac-verify.test.ts; Test 8 in pagarme-idempotent.test.ts as case 4; Test 9 covered by checkout-paths.test.ts case 'outbox emit'.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/payments/checkout-paths.test.ts tests/webhooks/pagarme-idempotent.test.ts tests/webhooks/perf.test.ts --run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>9 checkout+webhook tests green; webhook handler responds <100ms p95; inbox idempotency proven; re-fetch defense preserved; outbox 'payment.paid'|'payment.failed' emitted by worker (not handler); boleto path fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → /api/webhooks/pagarme | Untrusted external POST; HMAC verify BEFORE parse (raw-bytes); migrator role for cross-tenant write |
| client → /api/webhooks/pagarme | Same surface — attacker may probe; HMAC mitigates spoofing |
| Pagar.me API ↔ checkout action | Trusted endpoint; idempotency via X-Idempotency-Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Spoofing | Attacker forges webhook | mitigate | HMAC verify + Pitfall 1 raw-bytes; belt-and-suspenders re-fetch from Phase 1 PRESERVED in worker (Phase 1 RESEARCH §A8 was Basic Auth only; Phase 2 ADDS HMAC on top) |
| T-02-05-02 | Tampering | Replay attack on webhook | mitigate | payment_webhooks_inbox PK on gateway_event_id + ON CONFLICT DO NOTHING — replay collapses to single inbox row |
| T-02-05-03 | DoS | Webhook retry storm during outage | mitigate | Pitfall 2 — fast 200 from inbox INSERT absorbs the storm; worker processes async |
| T-02-05-04 | Tampering | Body normalization breaks HMAC | mitigate | Pitfall 1 — read raw arrayBuffer BEFORE JSON.parse |
| T-02-05-05 | Information disclosure | HMAC secret in logs | mitigate | Env-only; never log secret; Sentry data-scrubbing config (Phase 0) |
| T-02-05-06 | Tampering | Cart price snapshot omitted | mitigate | cart_addon_lines.price_brl_cents_snapshot — addon price freeze at add-time |
| T-02-05-07 | Information disclosure | Credit card token in our DB | accept | Pagar.me JS SDK browser-side tokenization; server never sees raw card data; only handles tokens |
| T-02-05-08 | Repudiation | Vendor disputes charge | mitigate | recordAudit + Pagar.me's own event log + payment_webhooks_inbox raw payload |
| T-02-05-09 | Tampering | Boleto sneaks in via validator gap | mitigate | Zod `method: z.enum(['pix','credit_card'])` only; types.ts has no boleto branch |
| T-02-05-SC | Tampering | No new npm packages | n/a | Pagar.me uses fetch (no SDK); HMAC uses node:crypto stdlib |
</threat_model>

<verification>
- Task 1 CHECKPOINT approved by operator with header name + encoding pinned
- `pnpm vitest tests/cart tests/webhooks tests/payments/checkout-paths.test.ts --run` all green
- `pnpm test --run` total ≥ 220 passing
- `pnpm tsc --noEmit` 0
- `pnpm lint` 0
- `pnpm check:all` 0
- Manual: PIX checkout on Pagar.me sandbox returns QR; webhook arrives; payments.status flips to paid; outbox row 'payment.paid' present
</verification>

<success_criteria>
- HMAC probe ran; header name pinned; ADR-0005 written
- FORN-08 + FORN-09 + FORN-10 + FORN-11 + FORN-12 all green
- Webhook hot path <100ms p95
- Boleto fully absent (AM-01)
- Outbox 'payment.paid'|'payment.failed' emitted by worker for Plan 02-06 to consume
</success_criteria>

<output>
Create `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-05-SUMMARY.md` when done. Include the HMAC header probe outcome verbatim + p95 webhook latency measurement.
</output>
