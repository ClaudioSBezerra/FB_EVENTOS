---
phase: 02-fornecedor-self-service-checkout-pix-cartao
plan: 08
type: execute
wave: 8
depends_on:
  - 02-02
  - 02-05
  - 02-06
  - 02-07
files_modified:
  - src/app/[slug]/portal/page.tsx
  - src/app/[slug]/portal/purchases/page.tsx
  - src/app/[slug]/portal/purchases/[paymentId]/page.tsx
  - src/app/[slug]/portal/settings/page.tsx
  - src/app/[slug]/portal/documents/page.tsx
  - src/components/portal/purchase-card.tsx
  - src/components/portal/refund-button.tsx
  - src/components/portal/consent-toggles.tsx
  - src/components/portal/document-upload.tsx
  - src/components/portal/portal-nav.tsx
  - src/lib/actions/portal.ts
  - src/lib/actions/consents.ts
  - src/lib/validators/consents.ts
  - src/db/schema/vendor_consents.ts
  - src/lib/storage/vendor-docs.ts
  - docs/RUNBOOK.md
  - tests/portal/render.test.ts
  - tests/lgpd/vendor-consent.test.ts
  - tests/e2e/walking-skeleton.spec.ts
  - tests/e2e/fixtures/phase2-gate-fixtures.ts
autonomous: false
requirements:
  - FORN-17
  - FORN-18
must_haves:
  truths:
    - "Vendor signs into `/[slug]/portal` and sees three sections: Purchases (FORN-17), Documents (FORN-17 upload), Settings (FORN-18 consent toggles). The portal is RLS-scoped — vendor only sees their own payments + contracts + docs"
    - "Purchase detail page `/[slug]/portal/purchases/{paymentId}` shows lot info, payment status, signed contract download (pre-signed MinIO URL TTL 15min), refund button (only if status='paid' AND no active refund_request) — the refund button submits to requestRefund Server Action from Plan 02-07. Per AM-05, segunda-via-de-boleto is OUT OF SCOPE for Phase 2 (AM-01 dropped boleto charges; no boleto data exists to serve; segunda-via lands in Phase 3 alongside boleto charge creation). The purchase-detail page renders only PIX/cartão receipt paths in Phase 2"
    - "Vendor settings page `/[slug]/portal/settings` shows 3 LGPD consent toggles (D-24): marketing | analytics | payment_data — each independent. Toggle ON inserts vendor_consents row with granted_at + consent_text snapshot + ip_address; toggle OFF UPDATEs revoked_at on the latest row of that consent_type and inserts an audit_log row. Soft-delete keeps audit but flags revoked"
    - "Documents page `/[slug]/portal/documents` shows existing vendor_documents (from Phase 1) + allows upload of additional docs via pre-signed PUT (reuses Phase 1 D-04 MinIO pattern) — vendor can upload bank statement, CNPJ, contrato social during Phase 2 piloto without re-running signup"
    - "**autonomous: false** — Task 5 is a CHECKPOINT mirroring Phase 1 D-14 Gate: operator-approved sandbox→production webhook auth flip (Basic Auth Phase 1 → HMAC Phase 2 per AM-02) + env var promotion + one real fornecedor PIX purchase on Trindade tenant in production. Done-done criterion for Phase 2"
    - "Playwright `tests/e2e/walking-skeleton.spec.ts` extended with `describe.serial('Phase 2 gate — fornecedor PIX vertical', ...)` block running the 5 sub-steps from VALIDATION.md §D-14-Equivalent Gate Test against Pagar.me sandbox + Trindade tenant"
  artifacts:
    - path: "src/app/[slug]/portal/page.tsx"
      provides: "Portal dashboard (vendor's tenant-scoped overview: purchases count, active reservations, pending refunds)"
    - path: "src/app/[slug]/portal/purchases/page.tsx"
      provides: "Tenant-scoped list of vendor's payments + lots + statuses"
    - path: "src/app/[slug]/portal/purchases/[paymentId]/page.tsx"
      provides: "Purchase detail + contract download + refund button"
      contains: "requestRefund"
    - path: "src/app/[slug]/portal/settings/page.tsx"
      provides: "LGPD consent toggles (D-24)"
      contains: "ConsentToggles"
    - path: "src/app/[slug]/portal/documents/page.tsx"
      provides: "Upload + list vendor_documents (extends Phase 1 cofre)"
    - path: "src/lib/actions/portal.ts"
      provides: "listVendorPurchases, getPurchaseDetail (thin actions + pure helpers)"
    - path: "src/lib/actions/consents.ts"
      provides: "recordConsent, revokeConsent — withTenantAction wrappers"
      contains: "vendor_consents"
    - path: "docs/RUNBOOK.md"
      provides: "Phase 2 — D-14 Gate Sandbox→Production Flip section appended (mirror of Phase 1 D-14 procedure)"
    - path: "tests/e2e/walking-skeleton.spec.ts"
      provides: "Phase 2 gate describe.serial block — 5 sub-steps end-to-end"
    - path: "tests/e2e/fixtures/phase2-gate-fixtures.ts"
      provides: "seedTrindadeFornecedor helper + simulatePagarmePaidWebhook helper + ensureSandboxEnv guard (mirror of d14-gate-fixtures.ts from 01-08)"
  key_links:
    - from: "src/components/portal/refund-button.tsx"
      to: "src/lib/actions/refunds.ts::requestRefund"
      via: "Server Action import"
      pattern: "requestRefund"
    - from: "src/components/portal/consent-toggles.tsx"
      to: "src/lib/actions/consents.ts::recordConsent"
      via: "Server Action import"
      pattern: "recordConsent"
    - from: "src/app/[slug]/portal/purchases/[paymentId]/page.tsx"
      to: "src/lib/actions/portal.ts::getContractDownloadUrl"
      via: "Server Action import + recordAudit BEFORE presignedGetObject (Phase 1 D-04 LGPD pattern)"
      pattern: "getContractDownloadUrl"
    - from: "src/lib/actions/portal.ts::getContractDownloadUrl"
      to: "src/lib/storage/minio.ts::presignedGetObject"
      via: "TTL 900s + Content-Disposition: attachment + audit row written BEFORE URL returned"
      pattern: "recordAudit\\('contract\\.download_minted'"
    - from: "src/components/portal/document-upload.tsx"
      to: "src/lib/storage/vendor-docs.ts::mintVendorDocUploadUrl"
      via: "Server Action import + browser-direct PUT to MinIO + statObject verification + audit"
      pattern: "mintVendorDocUploadUrl"
    - from: "tests/e2e/walking-skeleton.spec.ts"
      to: "Pagar.me sandbox + Trindade tenant"
      via: "ensureSandboxEnv guard NEVER flips production"
      pattern: "ensureSandboxEnv"
---

<objective>
Vendor portal + LGPD consent + Phase 2 D-14-equivalent gate. FORN-17 ships the self-service portal where a fornecedor sees their purchases, downloads contracts, uploads docs, and self-cancels (refund button wired to Plan 02-07's `requestRefund`). FORN-18 ships granular LGPD consent toggles (D-24) — 3 independent consent types stored in `vendor_consents` with versioned consent_text snapshots + audit trail. The Phase 2 gate mirrors Phase 1's D-14 procedure: a `describe.serial` Playwright block runs the full fornecedor PIX vertical against Pagar.me sandbox + Trindade tenant; passing it CHECKPOINTs the workflow for operator-approved env-var flip to production HMAC.

Purpose: Close FORN-17 + FORN-18; produce the Phase 2 "done-done" gate proving one real fornecedor PIX purchase end-to-end on Trindade.

Output: 5 portal pages + 4 components + 2 Server Action files + RUNBOOK Phase 2 D-14 section + walking-skeleton.spec.ts extension + fixtures + 2 integration test files + operator-approved CHECKPOINT.

**autonomous: false** — Task 5 is a CHECKPOINT for operator-approved sandbox→production env flip; cannot be auto-confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-VALIDATION.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-02-PLAN.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-05-PLAN.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-07-PLAN.md
@.planning/phases/01-organizadora-end-to-end-piloto-festa-de-trindade/01-08-SUMMARY.md
@src/app/[slug]/eventos/page.tsx
@src/app/[slug]/eventos/[eventId]/page.tsx
@src/lib/actions/safe-action.ts
@src/lib/storage/minio.ts
@src/lib/audit.ts
@src/db/schema/vendor_consents.ts
@src/db/schema/vendor_documents.ts
@docs/RUNBOOK.md
@tests/e2e/walking-skeleton.spec.ts
@tests/e2e/fixtures/d14-gate-fixtures.ts

<interfaces>
<!-- 02-PATTERNS.md -->
<!-- §portal pages (lines 481-560): mirror Phase 1 organizadora dashboard pages -->
<!-- §consent UI (lines 580-625): 3 independent checkbox toggles + recordAudit per change -->
<!-- §RUNBOOK Phase 2 D-14 section (lines 1280-1330): operator flip checklist mirroring Phase 1 -->
<!-- §walking-skeleton extension (lines 1340-1395): describe.serial block + ensureSandboxEnv guard -->

<!-- Phase 1 D-14 procedure reused -->
<!-- 01-08-SUMMARY.md describes the 4-step describe.serial gate fixtures shape; Phase 2 adds a 5th step (HMAC verify) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Portal layout + nav + 5 pages scaffolded with RLS-scoped data fetches</name>
  <files>src/app/[slug]/portal/page.tsx, src/app/[slug]/portal/purchases/page.tsx, src/app/[slug]/portal/purchases/[paymentId]/page.tsx, src/app/[slug]/portal/settings/page.tsx, src/app/[slug]/portal/documents/page.tsx, src/components/portal/portal-nav.tsx, src/components/portal/purchase-card.tsx, src/lib/actions/portal.ts, tests/portal/render.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 481-560 portal page analogs; lines 720-738 lot-assignments thin-action pattern for portal data fetches)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (FORN-17 in scope; D-19 SSE on planta; portal uses Phase 1 inline-popover-over-Radix pattern from Plan 01-07)
    - src/app/[slug]/eventos/page.tsx (full file — Phase 1 organizadora event list as analog for vendor purchases list)
    - src/app/[slug]/eventos/[eventId]/page.tsx (full file — Phase 1 event detail as analog for purchase detail)
    - src/lib/actions/safe-action.ts (full file — withTenantAction chain; pure-helper pattern)
    - src/lib/storage/minio.ts (full file — pre-signed GET URL for contract download)
    - src/db/schema/payments.ts (full file — joinable to lots + events + vendors)
    - src/db/schema/contracts.ts (full file — Phase 1; contract_id linked to payment)
  </read_first>
  <behavior>
    - Test 1 (portal dashboard RLS-scoped): vendor A in tenant T1 visits `/{T1.slug}/portal` → sees 3 stat cards (active purchases count, pending refunds, signed contracts); vendor A in tenant T2 sees their own stats only; cross-tenant data invisible (RLS-FORCED enforces)
    - Test 2 (purchases list): vendor A has 3 paid payments → list renders 3 purchase-card components with lot identifier + amount + status pill; status pill colors match getLotColorForStatus single-source-of-truth from Plan 01-07
    - Test 3 (purchase detail): vendor A visits `/{slug}/portal/purchases/{their_paymentId}` → sees lot info + contract download button (only renders if contract.signed_at IS NOT NULL); clicking download generates pre-signed MinIO URL via Server Action `getContractDownloadUrl` with TTL 900s + recordAudit('contract.download_minted')
    - Test 4 (purchase detail cross-vendor): vendor B tries to load `/{slug}/portal/purchases/{vendor_A_paymentId}` → 404 (not 403 to avoid information disclosure)
    - Test 5 (refund button visibility): payment.status='paid' AND no active refund_request → button visible; payment.status='paid' AND refund_requests row WHERE status IN ('pending','processing') exists → button replaced with "Estorno em andamento" banner; payment.status!='paid' → button absent
    - Test 6 (portal-nav highlight): `/{slug}/portal/settings` URL → settings nav item has aria-current='page'; other URLs have correct active state
  </behavior>
  <action>
    Create `src/lib/actions/portal.ts` exporting:
    - `listVendorPurchases()` — withTenantAction wrapper around pure helper `listVendorPurchasesCore(db, { vendor_id })` that SELECTs payments JOIN lots JOIN events JOIN contracts WHERE vendor_id=$1 ORDER BY paid_at DESC; LIMIT 50. Returns array of { paymentId, lotIdentifier, eventName, amountCentavos, paymentMethod, status, paidAt, contractSignedAt, hasActiveRefund } where hasActiveRefund is a subquery EXISTS on refund_requests
    - `getPurchaseDetail({ paymentId })` — withTenantAction returns single purchase + contract pre-signed download URL (TTL 900s) if signed
    - `getContractDownloadUrl({ contractId })` — withTenantAction validates contract.vendor_id === session.vendor_id; mints pre-signed GET URL; recordAudit('contract.download_minted', { contract_id }) BEFORE returning URL (Phase 1 D-04 pattern reused)

    Create `src/components/portal/portal-nav.tsx`:
    - Server component with 4 nav items: Dashboard | Purchases | Documents | Settings
    - Use `usePathname()` (client island) or `<Link active>` pattern; aria-current='page' on match
    - Inline-popover-over-Radix pattern (Phase 1 Plan 01-07 + 01-03 — no new @radix-ui deps; use simple `<Link>` + className conditional)

    Create `src/components/portal/purchase-card.tsx`:
    - Server component rendering { lotIdentifier, eventName, amountCentavos formatted as BRL, paymentMethod chip, status pill (color from getLotColorForStatus single-source from Plan 01-07), paid_at as date-fns formatted pt-BR, "Ver detalhes" Link to /[slug]/portal/purchases/{paymentId} }

    Create `src/app/[slug]/portal/page.tsx`:
    - Server component; calls listVendorPurchases() + aggregates: count(status='paid'), count(refund pending), count(contract signed)
    - Renders 3 stat cards + recent purchases (top 5) + portal-nav

    Create `src/app/[slug]/portal/purchases/page.tsx`:
    - Server component; calls listVendorPurchases() (all 50); renders purchase-card grid; empty state pt-BR "Você ainda não comprou nenhum lote — visite o marketplace"

    Create `src/app/[slug]/portal/purchases/[paymentId]/page.tsx`:
    - Server component; calls getPurchaseDetail({ paymentId: params.paymentId })
    - 404 if not found (RLS already filters; explicit notFound() per Next.js 15 idiom)
    - Renders: lot info card, payment info card, contract section (download button if signed; "Aguardando assinatura" otherwise), refund-button slot
    - Refund button as client island: `<RefundButton paymentId={paymentId} eligible={!hasActiveRefund && status==='paid'} />`

    Create `src/app/[slug]/portal/documents/page.tsx`:
    - Server component; lists vendor_documents (Phase 1 cofre rows) + renders DocumentUpload component (Task 2 ships)

    Fill `tests/portal/render.test.ts` with the 6 behavior tests using existing dual-tenant fixtures + factory helpers to seed 2 vendors × 2 tenants × payments/contracts/refund_requests rows.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/portal/render.test.ts --run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 5 portal pages render with RLS-scoped data (verified by Test 1 cross-tenant invisibility)
    - portal-nav uses aria-current='page'
    - purchase-card uses getLotColorForStatus single-source from Plan 01-07
    - getContractDownloadUrl writes audit_log row BEFORE returning URL (Phase 1 D-04 LGPD pattern)
    - cross-vendor purchase detail returns 404, not 403
    - All 6 tests green
  </acceptance_criteria>
  <done>Portal navigable; vendor sees their own RLS-scoped purchases + contracts; refund-button conditional on active refund state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refund button client island + Documents upload + integration with Plan 02-07 requestRefund</name>
  <files>src/components/portal/refund-button.tsx, src/components/portal/document-upload.tsx, src/lib/storage/vendor-docs.ts, tests/portal/render.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 738-748 refunds; lines 750-760 portal client islands)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-07-PLAN.md (full file — requestRefund + computeRefundPct semantics)
    - .planning/phases/01-organizadora-end-to-end-piloto-festa-de-trindade/01-04-SUMMARY.md (Phase 1 vendor_documents pre-signed PUT pattern)
    - src/lib/storage/minio.ts (full file — putObject + statObject; reuse exactly)
    - src/lib/actions/refunds.ts (full file — Plan 02-07; requestRefund signature)
    - src/components/eventos/planta-editor.tsx (full file — Phase 1 client island pattern + useTransition/useFormStatus pattern)
  </read_first>
  <behavior>
    - Test 1 (refund button confirm dialog): click button → inline dialog (no Radix dep) prompts "Confirma estorno? — política atual aplicará {pct}% sobre R$ {total}"; "Confirmar" submits, "Cancelar" closes — uses inline-popover-over-Radix pattern
    - Test 2 (refund button calls requestRefund): operator confirms → useTransition + Server Action call → success state shows "Estorno solicitado — aguardando processamento" + dispatches router.refresh()
    - Test 3 (refund button error): requestRefund rejects with 409 'refund already in progress' → error banner with pt-BR message; button stays disabled
    - Test 4 (Document upload happy): vendor clicks "Adicionar comprovante" → file input (max 25MB, accept .pdf/.jpg/.png) → server mints pre-signed PUT URL → browser uploads directly to MinIO → on PUT 200, calls Server Action `confirmDocumentUploaded({ key, kind: 'extrato_bancario' | 'cnpj' | 'contrato_social' | 'outro' })` which INSERTs vendor_documents row + recordAudit('vendor_document.uploaded')
    - Test 5 (Document upload size enforcement): vendor selects 30MB file → client-side validation rejects before pre-signed URL request; pre-signed URL also has 25MB size policy (MinIO `Content-Length-Range` policy)
    - Test 6 (Document download link): existing vendor_documents render with "Download" button → calls Server Action `getDocumentDownloadUrl({ documentId })` → pre-signed GET TTL 900s + audit (same pattern as Task 1's getContractDownloadUrl)
  </behavior>
  <action>
    Create `src/lib/storage/vendor-docs.ts` exporting:
    - `mintVendorDocUploadUrl({ vendor_id, tenant_id, filename, content_type, max_bytes })` — calls minioClient.presignedPutObject with Content-Length-Range policy {0, max_bytes (default 25*1024*1024)} → returns { url, key } where key=`vendor-docs/${tenant_id}/${vendor_id}/${uuid}-${slugify(filename)}`
    - `mintVendorDocDownloadUrl({ document_id })` — RLS-scoped SELECT vendor_documents; throws 404 cross-vendor; recordAudit('vendor_document.download_minted') BEFORE returning URL (Phase 1 D-04 pattern); returns pre-signed GET URL TTL 900s
    - `confirmDocumentUploaded` Server Action — INSERTs vendor_documents row with statObject verification (Phase 1 Plan 01-02 pattern reused: confirm MinIO actually received the bytes before persisting DB row)

    Create `src/components/portal/refund-button.tsx`:
    - Client component (`'use client'`)
    - Props: `{ paymentId: string; eligible: boolean; lotIdentifier: string; amountCentavos: number; eventStartsAt: string }`
    - State: `{ dialogOpen: boolean; submitting: boolean; error: string|null; success: boolean }`
    - On open: compute estimated refund_pct client-side using same DEFAULT_POLICY from Plan 02-07 (import { computeRefundPct, DEFAULT_POLICY } — pure function) so dialog shows the exact pct before server roundtrip
    - On confirm: `useTransition` + import { requestRefund } from '@/lib/actions/refunds' (server action) → handle success/error
    - Inline dialog uses absolute-positioned div + backdrop (Phase 1 inline-popover-over-Radix; no @radix-ui/dialog dep added)
    - aria-* attributes: aria-haspopup='dialog', aria-expanded, role='dialog' on open

    Create `src/components/portal/document-upload.tsx`:
    - Client component
    - File input with accept='.pdf,.jpg,.jpeg,.png' + max 25MB client-side check
    - On selection: calls server action mintVendorDocUploadUrl → PUT via fetch (browser uploads directly to MinIO; never proxies through Next.js) → on 200, calls confirmDocumentUploaded
    - Optimistic UI: shows uploading progress; on success, parent revalidates

    Extend `tests/portal/render.test.ts` with the 6 new tests above OR create dedicated `tests/portal/refund-and-upload.test.ts` if mixing concerns. Use Playwright Component Testing via vitest-browser-react if already in tree; otherwise React Testing Library + vi.spyOn the server action imports.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/portal --run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/storage/vendor-docs.ts` exports `mintVendorDocUploadUrl`, `mintVendorDocDownloadUrl`, `confirmDocumentUploaded`
    - Browser uploads directly to MinIO via pre-signed PUT — Next.js server never proxies file bytes (Phase 1 D-04 perf + cost rationale)
    - Audit row written BEFORE pre-signed URL returned (download), AFTER statObject verification (upload)
    - Refund button is client island; imports computeRefundPct + DEFAULT_POLICY from src/lib/refund/policy.ts (Plan 02-07) — same constants both sides
    - No new @radix-ui/* dep added (verify via `git diff package.json` → only inline-popover code)
    - All 6 new tests green
  </acceptance_criteria>
  <done>Refund button + document upload wired; vendor can self-cancel + upload extra docs in portal; no new Radix dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LGPD granular consent UI + recordConsent/revokeConsent Server Actions + vendor_consents schema review</name>
  <files>src/components/portal/consent-toggles.tsx, src/lib/actions/consents.ts, src/lib/validators/consents.ts, src/db/schema/vendor_consents.ts, tests/lgpd/vendor-consent.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 580-625 consent UI; lines 1145-1170 vendor_consents schema)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-24 — 3 consent types, soft-delete via revoked_at, audit per change)
    - .planning/phases/00-foundation-stack-lock-anti-pitfall-hardening/00-05-SUMMARY.md (full file — Phase 0 consent_records pattern with consent_text snapshot + versioning)
    - src/db/schema/consent_records.ts (full file — Phase 0 baseline; vendor_consents follows same shape)
    - src/lib/actions/safe-action.ts (full file)
    - src/lib/audit.ts (full file — recordAudit signature)
  </read_first>
  <behavior>
    - Test 1 (recordConsent fresh): vendor toggles 'marketing' ON for the first time → INSERT vendor_consents row { vendor_id, consent_type='marketing', granted_at=now(), revoked_at=NULL, ip_address=request.ip, consent_text='version snapshot' } + recordAudit('consent.granted', { consent_type })
    - Test 2 (revokeConsent): vendor toggles 'marketing' OFF → UPDATE vendor_consents SET revoked_at=now() WHERE vendor_id=$ AND consent_type='marketing' AND revoked_at IS NULL + recordAudit('consent.revoked')
    - Test 3 (re-grant after revoke): vendor toggles 'marketing' OFF, then ON again → previous row's revoked_at preserved; NEW row inserted with new granted_at + new consent_text version snapshot
    - Test 4 (3 independent toggles): marketing ON, analytics OFF, payment_data ON → 2 vendor_consents rows + 0 for the OFF one; audit_log shows 3 entries (2 granted + 0 revoked since baseline)
    - Test 5 (consent_text version pinning): change `LGPD_CONSENT_TEXT_VERSION` env to v2 + re-grant marketing → new row has consent_text snapshot from v2; old row stays v1 (versioning preserved for audit)
    - Test 6 (RLS-scoped): vendor A in tenant T1 sees only their own consents; vendor B's consents in T1 invisible (tenant_isolation FORCE RLS)
    - Test 7 (consent UI renders current state): initial page load → SELECT current consent state per type → 3 toggles render with correct ON/OFF
    - Test 8 (toggle audit chain — LGPD compliance): every state change writes audit_log entry; LGPD inspector can reconstruct full consent timeline per vendor
  </behavior>
  <action>
    Audit `src/db/schema/vendor_consents.ts` (created in Plan 02-01) ensuring schema matches D-24:
    - Columns: id (uuid PK), tenant_id (FK), vendor_id (FK), consent_type (enum: marketing|analytics|payment_data), granted_at (timestamptz not null), revoked_at (timestamptz nullable), ip_address (inet nullable), consent_text (text not null — pt-BR snapshot at grant time)
    - FORCE RLS + tenant_isolation policy
    - COMMENT ON COLUMN ... 'PII: ip_address; consent_type'
    - Index on (vendor_id, consent_type) WHERE revoked_at IS NULL for current-state lookups

    Create `src/lib/validators/consents.ts`:
    - `recordConsentSchema = z.object({ consent_type: z.enum(['marketing','analytics','payment_data']) })`
    - `revokeConsentSchema = z.object({ consent_type: z.enum(['marketing','analytics','payment_data']) })`

    Create `src/lib/actions/consents.ts` exporting:
    - `recordConsent({ consent_type })` — withTenantAction wrapper:
      - Pure helper inserts new vendor_consents row with consent_text=getCurrentConsentText() (read from env LGPD_CONSENT_TEXT_VERSION + lookup table OR hardcoded constant module — match Phase 0 Plan 05 pattern)
      - recordAudit('consent.granted', { consent_type, version }) — version derived from consent_text
    - `revokeConsent({ consent_type })` — withTenantAction wrapper:
      - Pure helper UPDATEs the latest non-revoked row → SET revoked_at=now()
      - recordAudit('consent.revoked', { consent_type })
    - `getCurrentConsents()` — withTenantAction returns map { marketing: boolean, analytics: boolean, payment_data: boolean } based on existence of vendor_consents row WHERE revoked_at IS NULL

    Create `src/components/portal/consent-toggles.tsx`:
    - Client component
    - Props: `{ initial: { marketing: boolean; analytics: boolean; payment_data: boolean }; consentText: string }`
    - 3 toggles (HTML5 input[type=checkbox] styled — no @radix-ui/switch); each calls recordConsent or revokeConsent via useTransition
    - Show consent_text snapshot below toggles ("Ao ativar, você concorda com: ...") in pt-BR
    - On error: toast pt-BR "Não foi possível atualizar — tente novamente" (use existing toast pattern from Phase 1 or simple inline error span)

    Fill `tests/lgpd/vendor-consent.test.ts` with the 8 behavior tests using tenant fixtures + audit_log inspection + cross-vendor RLS test.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/lgpd/vendor-consent.test.ts --run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `vendor_consents` schema has versioned `consent_text` snapshot + soft-delete via `revoked_at`
    - Audit_log row per state change (granted + revoked) with consent_type
    - 3 independent toggles in UI; no @radix-ui/switch dep
    - LGPD compliance reconstructable: timeline of consent state per vendor available via SELECT vendor_consents ORDER BY granted_at
    - All 8 tests green
  </acceptance_criteria>
  <done>FORN-18 LGPD granular consent live; audit chain complete; consent_text version pinning preserves the wording each vendor agreed to.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: RUNBOOK Phase 2 — D-14 Gate Sandbox→Production Flip section + Playwright walking-skeleton extension + Phase 2 gate fixtures</name>
  <files>docs/RUNBOOK.md, tests/e2e/walking-skeleton.spec.ts, tests/e2e/fixtures/phase2-gate-fixtures.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-VALIDATION.md (full file — §D-14-Equivalent Gate Test 5 sub-steps; §Manual-Only Verifications)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md (lines 814-872 §Validation Architecture; AM-02 HMAC probe outcome must be recorded here)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (AM-02 HMAC probe-verify at execute-time; AM-01 boleto removed; CANONICAL_DOMAIN=https://eventos.fbtax.cloud)
    - .planning/phases/01-organizadora-end-to-end-piloto-festa-de-trindade/01-08-SUMMARY.md (full file — Phase 1 D-14 gate fixtures + describe.serial pattern as direct analog)
    - docs/RUNBOOK.md (full file — Phase 1 D-14 Gate section is the template for Phase 2 D-14 Gate section)
    - tests/e2e/walking-skeleton.spec.ts (full file — Phase 1 4-step describe.serial gate; Phase 2 adds a 5th describe.serial block as a sibling)
    - tests/e2e/fixtures/d14-gate-fixtures.ts (full file — seedTrindadeTenant + simulateZapsignWebhook + simulatePagarmeWebhook + ensureSandboxEnv helpers; Phase 2 fixtures mirror this structure)
  </read_first>
  <behavior>
    - This task ships the structural artifacts ONLY; the actual operator-approved flip happens in Task 5 CHECKPOINT
    - RUNBOOK section is parseable markdown with operator checklist + rollback steps
    - Playwright spec runs in CI (project: phase2-gate, separate from chromium) against Pagar.me sandbox
    - Fixtures NEVER touch production via ensureSandboxEnv guard (mirrors Phase 1 pattern)
  </behavior>
  <action>
    Append to `docs/RUNBOOK.md` a new section **"Phase 2 — D-14 Gate Sandbox→Production Flip"**:
    - **Pre-flip checklist (operator must check all):**
      1. Phase 2 plans 02-01..02-07 SUMMARY.md files exist and all tests green
      2. `pnpm playwright test --project=phase2-gate` passes against sandbox (5/5 sub-steps)
      3. HMAC probe-test output recorded in `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-05-SUMMARY.md` (header name + algorithm pinned)
      4. `PAGARME_WEBHOOK_SIGNING_SECRET` provisioned in Coolify production env vars
      5. `WAITLIST_JWT_SECRET` provisioned in Coolify production env vars (32+ chars)
      6. Trindade tenant exists in production with at least 1 active event + 1 lot
      7. Backup of production Postgres taken via `pg_dump --schema=fbeventos --file=backup-pre-phase2-$(date +%F).sql`
    - **Flip procedure (operator-approved, manual):**
      1. SSH to Coolify dashboard → FB_EVENTOS app → Env vars tab
      2. Set `PAGARME_WEBHOOK_AUTH_MODE=hmac` (Phase 1 was `basic`); confirm Coolify shows pending redeploy
      3. Trigger redeploy (tag = current semver; never `:latest`)
      4. Healthcheck `/api/health` returns 200 within 60s
      5. In Pagar.me dashboard: rotate webhook secret to value from Coolify env; confirm sample webhook delivery test passes (200 from our endpoint with valid HMAC)
    - **Smoke test (within 15 min of flip):**
      1. Operator (using own fornecedor test account) navigates to `https://eventos.fbtax.cloud/trindade/marketplace`
      2. Picks a lot priced minimally (R$ 50 max — sandbox-like value for prod smoke)
      3. PIX checkout → operator pays via test phone PIX
      4. Within 5 min: webhook arrives, payment.paid processed, lot=sold, recibo email arrives in operator's mailbox
      5. Confirm audit_log shows the full chain (webhook received → outbox emitted → handler ran → lot sold)
    - **Rollback (if smoke fails):**
      1. Set `PAGARME_WEBHOOK_AUTH_MODE=basic` in Coolify
      2. Redeploy previous semver tag
      3. Pagar.me dashboard: re-set webhook to Basic Auth from previous secret
      4. Restore from `backup-pre-phase2-*.sql` if data corruption
    - **Done-done signal:** operator-approved smoke pass; commit a marker file `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/D14-PHASE2-APPROVED-${date}.txt`

    Create `tests/e2e/fixtures/phase2-gate-fixtures.ts` exporting (mirror of d14-gate-fixtures.ts):
    - `seedTrindadeFornecedor()` — creates a test fornecedor with verified email + member of Trindade tenant org + status='approved' (skip the org approval flow for the gate)
    - `simulatePagarmePaidWebhook({ chargeId, orderId, amount })` — POSTs to /api/webhooks/pagarme with a VALID HMAC signature using `PAGARME_WEBHOOK_SIGNING_SECRET` from process.env (test-only secret, never the prod one). Header name pinned to whatever AM-02 probe captured (read from a generated file `src/lib/pagarme/hmac-header-name.generated.ts` Plan 02-05 emits)
    - `simulatePagarmePaymentFailed({ chargeId, orderId })` — same but with payment.failed event
    - `ensureSandboxEnv()` — asserts `NODE_ENV !== 'production'` AND `PAGARME_API_URL` matches sandbox URL pattern; throws if either fails (guarantee tests cannot accidentally flip production)
    - `cleanupPhase2Fixtures()` — DELETE FROM lot_reservations + cart_addon_lines + waitlist_token_uses + outbox_events + payment_webhooks_inbox WHERE created_at > test_start (clean test data)

    Extend `tests/e2e/walking-skeleton.spec.ts` adding a new sibling `describe.serial` block per VALIDATION §D-14-Equivalent Gate Test (mirror of Phase 1's describe.serial pattern):

    ```
    test.describe.serial('Phase 2 gate — fornecedor PIX vertical', () => {
      test.beforeAll(ensureSandboxEnv)
      test.beforeEach(seedTrindadeFornecedor)
      test.afterEach(cleanupPhase2Fixtures)

      test('Sub-step 1: fornecedor signs up via /[slug]/fornecedor/cadastro', ...)
      test('Sub-step 2: fornecedor opens marketplace + selects lot', ...)
      test('Sub-step 3: fornecedor checkouts PIX via Pagar.me sandbox', ...)
      test('Sub-step 4: HMAC-signed webhook arrives + payment processes via outbox', ...)
      test('Sub-step 5: lot=sold + recibo email + audit chain complete', ...)
    })
    ```

    Update `playwright.config.ts` registering project 'phase2-gate' with serial mode + `testMatch: /walking-skeleton.spec.ts/` + `testIgnore: !(/Phase 2 gate/)` if Playwright supports that filtering; otherwise tag tests with `@phase2-gate` and use `--grep @phase2-gate`.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm vitest tests/e2e/fixtures/phase2-gate-fixtures.test.ts --run</automated>
    <note>
      Task 4 structural verify only — CI-safe. It exercises the FIXTURE EXPORTS via a Vitest unit test that imports each helper, mocks pg + fetch, and asserts: ensureSandboxEnv throws on NODE_ENV=production; seedTrindadeFornecedor produces a valid insert payload; simulatePagarmePaidWebhook generates a signed body matching the hmac-header-name.generated.ts contract IF that file exists, else skips the signature assertion with a clear "Plan 02-05 probe not yet run" message. The FULL Playwright phase2-gate suite (which needs live Pagar.me sandbox credentials + the 02-05 probe outcome) runs in Task 5's CHECKPOINT, not here. Create `tests/e2e/fixtures/phase2-gate-fixtures.test.ts` alongside the .ts file with these unit assertions.
    </note>
  </verify>
  <acceptance_criteria>
    - `docs/RUNBOOK.md` contains a "Phase 2 — D-14 Gate Sandbox→Production Flip" section with pre-flip checklist + flip procedure + smoke test + rollback
    - `tests/e2e/fixtures/phase2-gate-fixtures.ts` exports 5 helpers
    - `tests/e2e/fixtures/phase2-gate-fixtures.test.ts` unit-test verifies fixture exports (no network, no secrets) — runs green in CI
    - ensureSandboxEnv throws when NODE_ENV=production (asserted by the unit test above)
    - `tests/e2e/walking-skeleton.spec.ts` describe.serial block is structurally present (the FILE compiles and exposes the 5 sub-step test names) — its actual ASSERTIONS run in Task 5 CHECKPOINT with sandbox creds
    - HMAC header name + installments shape are read from `src/lib/pagarme/hmac-header-name.generated.ts` + `src/lib/pagarme/installments-shape.generated.ts` (Plan 02-05 probe outcomes) — NEVER hardcoded
    - Playwright project 'phase2-gate' configured in playwright.config.ts
  </acceptance_criteria>
  <done>Structural Phase 2 D-14 gate scaffolded; unit-test verifies fixture contracts in CI. Full sandbox run + production flip happen in Task 5 CHECKPOINT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] Phase 2 D-14-equivalent CHECKPOINT — operator-approved sandbox→production webhook auth flip + smoke test</name>
  <files>(no code changes — manual operator workflow)</files>
  <read_first>
    - docs/RUNBOOK.md (full file — focus on "Phase 2 — D-14 Gate Sandbox→Production Flip" section added in Task 4)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-VALIDATION.md (full file — §Manual-Only Verifications matrix)
    - .planning/phases/01-organizadora-end-to-end-piloto-festa-de-trindade/01-08-SUMMARY.md (full file — Phase 1 D-14 flip outcome as reference for what success looks like)
  </read_first>
  <what-built>
    Operator-approved production flip of Pagar.me webhook authentication from Phase 1 Basic Auth → Phase 2 HMAC (AM-02) on Trindade tenant. Done-done criterion: one real fornecedor PIX purchase end-to-end without intervention.
  </what-built>
  <how-to-verify>
    Operator (human):
    1. Confirm all 7 pre-flip checklist items in RUNBOOK Phase 2 D-14 section
    2. **Run the full Playwright phase2-gate suite against Pagar.me sandbox FIRST (this is the dress rehearsal):** `pnpm playwright test --project=phase2-gate --reporter=list`. Requires `PAGARME_SANDBOX_API_KEY` + `PAGARME_WEBHOOK_SIGNING_SECRET` + `WAITLIST_JWT_SECRET` in the local env. All 5 sub-steps must pass green. This is the gate that Plan 02-08 Task 4 deferred from its automated verify.
    3. Execute the 5 flip procedure steps (Coolify env var set → redeploy → healthcheck → Pagar.me dashboard rotate → confirm sample webhook delivery)
    4. Within 15 minutes of flip, run the 5 smoke test steps using operator's own test fornecedor on production Trindade tenant
    5. On smoke success: commit `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/D14-PHASE2-APPROVED-${date}.txt` with operator name + timestamp + smoke screenshot reference
    6. On smoke failure: execute rollback steps; reopen Plan 02-08 or relevant prior plan; flag failure mode in CONTEXT.md as a new AM-XX

    **DO NOT proceed to Phase 3 planning until this CHECKPOINT is approved.** State remains "Phase 2 plans complete, awaiting D-14 flip" until the marker file lands on main.
  </how-to-verify>
  <resume-signal>Type "phase 2 d-14 approved YYYY-MM-DD" with the marker file path. State transitions to "Phase 2 COMPLETE".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendor session ↔ portal pages | All portal Server Actions wrap in withTenantAction; cross-vendor 404 not 403 to prevent information disclosure |
| Vendor browser ↔ MinIO | Pre-signed PUT/GET with TTL 900s + Content-Length-Range policy; vendor uploads directly, server never proxies file bytes (perf + cost) |
| Vendor browser ↔ consent state | recordConsent / revokeConsent re-validate session.vendor_id on every call; audit_log row per change |
| Operator (production console) ↔ Pagar.me production env | Coolify env vars (gated UI access); secret rotation in Pagar.me dashboard (gated by 2FA) |
| Playwright phase2-gate ↔ Pagar.me sandbox | ensureSandboxEnv guard throws if NODE_ENV=production OR PAGARME_API_URL not sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Information disclosure | Cross-vendor purchase visibility | mitigate | RLS-FORCED enforces; explicit 404 on session.vendor_id !== purchase.vendor_id (not 403 — avoid leaking existence) |
| T-02-08-02 | Information disclosure | Contract download by stolen vendor link | mitigate | Pre-signed URL TTL 900s; recordAudit row written BEFORE URL returned; auditor can correlate downloads (Phase 1 D-04) |
| T-02-08-03 | Tampering | Consent_text version drift makes old consents unverifiable | mitigate | consent_text snapshot stored on each vendor_consents row; vendor_consents.consent_text is a snapshot, not FK to a versioned table; LGPD inspector can reconstruct exact text per consent |
| T-02-08-04 | Spoofing | Refund button submitted by another tab/window after vendor B logs in | mitigate | requestRefund re-validates session.vendor_id === payment.vendor_id (Plan 02-07); React 19 form actions tied to session cookie |
| T-02-08-05 | Tampering | E2E gate accidentally flips production | mitigate | ensureSandboxEnv() throws on NODE_ENV=production; PAGARME_API_URL pattern asserted; Playwright project 'phase2-gate' separate from chromium |
| T-02-08-06 | DoS | Vendor uploads 25MB×N files filling MinIO | accept | Phase 2 piloto volume is small; Phase 4 adds quota |
| T-02-08-07 | Repudiation | Operator-approved D-14 flip — no audit trail | mitigate | Marker file `D14-PHASE2-APPROVED-${date}.txt` committed with operator name + timestamp + screenshot ref; git history = audit trail |
| T-02-08-SC | n/a | No new npm packages | n/a | All UI uses existing shadcn primitives + inline-popover pattern |
</threat_model>

<verification>
- All 6 + 6 + 8 unit/integration tests green (Tasks 1-3)
- `pnpm playwright test --project=phase2-gate` green (Task 4) against sandbox
- `pnpm tsc --noEmit` 0 / `pnpm lint` 0
- Bundle size delta minimal (no new @radix-ui/* dep; verify via `pnpm build --turbo` + bundle analyzer)
- Operator CHECKPOINT approved (Task 5) — marker file landed on main
- RUNBOOK Phase 2 D-14 section reviewable by next operator without context loss
</verification>

<success_criteria>
- FORN-17 end-to-end: vendor sees purchases + contracts + uploads docs + self-refunds via portal (all RLS-scoped)
- FORN-18 end-to-end: 3 LGPD consent toggles + audit chain + consent_text version pinning per vendor
- Phase 2 D-14-equivalent gate passes against sandbox (5 sub-steps green)
- Operator-approved production flip; first real fornecedor PIX purchase processed via HMAC webhook
- ROADMAP Phase 2 marked Complete with operator-approved marker
</success_criteria>

<output>
Create `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-08-SUMMARY.md` when done. Include:
- The operator-approved D-14 flip marker file path + commit hash
- HMAC header name probe outcome (recorded in Plan 02-05; copy here for SUMMARY-self-contained reference)
- A screenshot or audit_log excerpt showing one real fornecedor's PIX purchase completing end-to-end on production Trindade tenant
- Test counts: full Phase 2 suite size + Playwright phase2-gate counts
</output>
