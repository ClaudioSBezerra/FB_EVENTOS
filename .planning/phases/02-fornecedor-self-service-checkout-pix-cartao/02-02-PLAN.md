---
phase: 02-fornecedor-self-service-checkout-pix-cartao
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/actions/signup-fornecedor.ts
  - src/lib/validators/signup-fornecedor.ts
  - src/app/[slug]/fornecedor/cadastro/page.tsx
  - src/components/fornecedor/signup-form.tsx
  - src/lib/actions/marketplace.ts
  - src/app/[slug]/marketplace/page.tsx
  - src/app/[slug]/marketplace/[eventId]/page.tsx
  - src/components/marketplace/event-card.tsx
  - tests/fornecedor/signup.test.ts
  - tests/marketplace/list.test.ts
autonomous: true
requirements:
  - FORN-01
  - FORN-02
must_haves:
  truths:
    - "Fornecedor visits /{slug}/fornecedor/cadastro WITHOUT a session, submits email+password+legal_name+trade_name+CNPJ+phone+consents, server creates Better Auth user (if needed), adds them as member of the tenant org, inserts vendor row with status='pending', records 3 LGPD consents (D-24), and enqueues email.send-status-update with event='signup_fornecedor'"
    - "Logged-in fornecedor visits /{slug}/marketplace and sees only events from THIS tenant with status='published'; cross-tenant events are invisible (RLS asserted)"
    - "Vendor row + member row land in the same Server Action — partial failure leaves no orphan rows (use existing recordAuditOutOfBand pattern from Phase 1 payments.ts:404-414)"
    - "Same human signing up with same CNPJ on a different tenant succeeds — produces a second vendor row (D-22)"
  artifacts:
    - path: "src/lib/actions/signup-fornecedor.ts"
      provides: "signupFornecedor pure-helper + thin-action; slug → resolveTenantBySlug → auth.api.addMember → INSERT vendors + recordConsent (per type) + enqueueJob"
      contains: "export async function signupFornecedorForTenant"
    - path: "src/app/[slug]/fornecedor/cadastro/page.tsx"
      provides: "Server Component renders SignupForm with tenant.name in header; PUBLIC (no session check)"
    - path: "src/components/fornecedor/signup-form.tsx"
      provides: "Client Component — react-hook-form + Zod resolver; CNPJ input from src/components/fornecedores/cnpj-input.tsx; 3 LGPD checkboxes (marketing/analytics/payment_data)"
    - path: "src/lib/actions/marketplace.ts"
      provides: "listOpenEventsInTenant pure-helper; filter events.status='published' AND deleted_at IS NULL"
    - path: "src/app/[slug]/marketplace/page.tsx"
      provides: "Server Component using Pattern S9 boilerplate (02-PATTERNS.md lines 1445-1459); renders EventCard list"
    - path: "src/app/[slug]/marketplace/[eventId]/page.tsx"
      provides: "Event detail Server Component with 'Ver planta' CTA linking to /{slug}/marketplace/{eventId}/planta (rendered in Plan 02-03)"
    - path: "src/components/marketplace/event-card.tsx"
      provides: "Per-event card showing name + dates + %available + link to planta"
  key_links:
    - from: "src/app/[slug]/fornecedor/cadastro/page.tsx"
      to: "signupFornecedor Server Action"
      via: "form submit"
      pattern: "signupFornecedor\\("
    - from: "src/lib/actions/signup-fornecedor.ts"
      to: "auth.api.addMember"
      via: "Better Auth org plugin"
      pattern: "addMember\\(\\{ organizationId"
    - from: "src/app/[slug]/marketplace/page.tsx"
      to: "withTenant"
      via: "RLS"
      pattern: "withTenant\\(tenant\\.id"
---

<objective>
End-to-end fornecedor onboarding slice: a brand-new fornecedor lands on `/{slug}/fornecedor/cadastro`, fills the form, becomes a tenant-scoped vendor + Better Auth org member, then sees the tenant's open events at `/{slug}/marketplace`. After this plan ships, a real Trindade fornecedor can self-sign-up and discover the Festa de Trindade event — they cannot yet click a lot (Plan 02-03) or pay (Plan 02-05), but the "doorway" is open.

Purpose: Close FORN-01 + FORN-02; eliminate the manual organizadora-side fornecedor creation step on a real event.

Output: Public signup page + signup Server Action + marketplace list pages + filled-in tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-RESEARCH.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md
@.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-01-PLAN.md
@src/lib/actions/fornecedores.ts
@src/lib/actions/consent.ts
@src/lib/actions/eventos.ts
@src/lib/actions/brasilapi.ts
@src/lib/actions/safe-action.ts
@src/lib/tenant.ts
@src/auth/server.ts
@src/components/auth/signup-form.tsx
@src/components/fornecedores/cnpj-input.tsx
@src/app/(auth)/signup/page.tsx
@src/app/[slug]/eventos/page.tsx
@src/jobs/raw-sql-from-tenant-db.ts
@src/jobs/enqueue.ts
@src/lib/audit.ts

<interfaces>
<!-- Patterns from 02-PATTERNS.md -->
<!-- Group D — Server Actions analog: src/lib/actions/fornecedores.ts (createVendorInTenant) lines 50-90 of 02-PATTERNS.md -->
<!-- Group G — Pages analog: src/app/(auth)/signup/page.tsx + src/app/[slug]/eventos/page.tsx -->
<!-- Pattern S2 caveat (line 1322): signup-fornecedor runs BEFORE activeOrganizationId is set — use actionClient (not withTenantAction) -->

From src/auth/server.ts (Better Auth org plugin surface — read existing file):
  - `auth.api.getFullOrganization({ headers, query: { organizationSlug } }) → { id, name, slug, ... }`
  - `auth.api.addMember({ body: { organizationId, userId, role } })` server-only
  - `signUp.email({ email, password, name })` (Better Auth client API, used from client component)

From src/lib/actions/fornecedores.ts (Phase 1 pattern):
  - `createVendorInTenant(db, tenantId, input, userId)` — exact reuse target; Phase 2 wraps in the signup orchestrator
  - `lookupCNPJCore(cnpj)` Layer 2 with degrade-with-warning

From src/lib/actions/consent.ts:
  - `recordConsentMetadata({ consentVersion, consentText })` — Phase 0 LGPD baseline
  - `extractClientIp(headerMap)` lines 45-56 — reuse for vendor_consents.ip_address
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signupFornecedor Server Action + signup form + public page</name>
  <files>src/lib/actions/signup-fornecedor.ts, src/lib/validators/signup-fornecedor.ts, src/app/[slug]/fornecedor/cadastro/page.tsx, src/components/fornecedor/signup-form.tsx, tests/fornecedor/signup.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 760-783 signup-fornecedor pattern; lines 1106-1166 signup form analog; lines 1310-1322 Pattern S2 caveat about activeOrganizationId-not-set yet)
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-CONTEXT.md (D-21 D-22 D-23 D-24)
    - src/lib/actions/fornecedores.ts (createVendorInTenant — full file)
    - src/lib/actions/consent.ts (recordConsentMetadata + extractClientIp — full file)
    - src/lib/actions/safe-action.ts (actionClient pattern for unauthed actions — full file)
    - src/lib/validators/vendor.ts (existing vendor schema — full file)
    - src/components/auth/signup-form.tsx (signup form analog — full file)
    - src/components/fornecedores/cnpj-input.tsx (CNPJ input component — full file)
    - src/lib/tenant.ts (resolveTenantBySlug — full file)
    - src/auth/server.ts (Better Auth + org plugin config — full file)
  </read_first>
  <behavior>
    - Test 1 (happy path): Form submit with valid email, password (≥10 chars), name, legal_name, trade_name, cnpj (validated via mocked BrasilAPI Layer 2), phone, all 3 consents granted → Server Action returns ok=true; vendor row exists in tenant DB with status='pending' (D-23); auth.user + auth.member rows exist; 3 vendor_consents rows inserted; recordAudit row with action='vendor.signed_up' present; email.send-status-update job enqueued with event='signup_fornecedor'
    - Test 2 (D-22 cross-tenant CNPJ): Same human (same Better Auth user) signs up in tenant_A then tenant_B with same CNPJ → 2 vendor rows (one per tenant), 2 member rows (one per org), 1 auth.user row reused. NOT a uniqueness error.
    - Test 3 (CNPJ degrade-with-warning): BrasilAPI returns 5xx → vendor row inserted with `cnpj_verified=false`, recordAudit notes degrade. Signup still completes (Phase 1 D-16 pattern).
    - Test 4 (missing required consent): payment_data consent unchecked → Zod rejects, no DB writes happen.
    - Test 5 (LGPD audit on signup): vendor_consents rows have ip_address from x-forwarded-for header (via extractClientIp); consent_text snapshot stored; consent_version pinned.
  </behavior>
  <action>
    Create `src/lib/validators/signup-fornecedor.ts` Zod schema: email (email), password (min 10), name (min 2), legal_name (min 2), trade_name (min 1), cnpj (cnpjSchema from Phase 1), phone (br-mobile pattern), consents object { marketing: boolean, analytics: boolean, payment_data: boolean — last MUST be true; first two free }. Add `slug` (tenant slug) and `consent_version` constant from src/lib/email/templates/shared.ts (or fork a Phase 2 LGPD_CONSENT_VERSION_V1).

    Create `src/lib/actions/signup-fornecedor.ts` per D-21/D-22/D-23/D-24 + 02-PATTERNS.md §signup-fornecedor:
    - Use `actionClient` (NOT `withTenantAction`) — no active org yet (Pattern S2 caveat).
    - `signupFornecedorForTenant(slug, input, headers)` pure helper:
      1. `resolveTenantBySlug(slug)` → 404 if not found
      2. `auth.api.getFullOrganization({ headers: passthrough headers, query: { organizationSlug: slug }})` → returns org id
      3. Check if Better Auth user exists by email (auth admin API) — if yes, reuse; if no, create via `auth.api.signUpEmail({ body: { email, password, name }})` (server-only). Surfaces email-already-used as a soft reuse, not error (D-22).
      4. `auth.api.addMember({ body: { organizationId: org.id, userId: newOrExistingUser.id, role: 'member' }})` — adds membership for this tenant.
      5. Wrap remaining work in `withTenant(tenant.id, async (db) => { ... })`:
         - Call existing `createVendorInTenant(db, tenant.id, vendorInput, userId)` (Phase 1) — this handles CNPJ lookup + degrade-with-warning + audit + email enqueue.
         - For each consent type (marketing, analytics, payment_data) where input.consents[type] === true: INSERT into vendor_consents { vendor_id: createdVendor.id, consent_type, consent_text: CONSENT_TEXT_V1[type], consent_version: LGPD_CONSENT_VERSION_V1, ip_address: extractClientIp(headers) } + recordAudit({action: 'vendor_consent.granted', ...}).
         - Return { vendor_id: createdVendor.id, member_id, redirect_to: `/${slug}/portal` }.
    - Thin action: `signupFornecedor` Server Action wrapper exposed to the client form; reads headers via Next.js `headers()`.

    Create `src/app/[slug]/fornecedor/cadastro/page.tsx` Server Component (PUBLIC — no session check; resolveTenantBySlug + render form with tenant.name header). 404 if slug unknown.

    Create `src/components/fornecedor/signup-form.tsx` Client Component using react-hook-form + Zod resolver. Layout fields per behavior list. Reuse `<CNPJInput />` from src/components/fornecedores/cnpj-input.tsx (already wired to Layer 1 client-side BrasilAPI). 3 LGPD checkboxes use the existing pattern from src/components/auth/signup-form.tsx lines 117-219. Submit handler awaits `signupFornecedor(slug, values)`; on success, `router.push(`/${slug}/portal`)` (placeholder — Plan 02-08 fills in portal page; for now route exists from Plan 02-04 SSE handler infra but a transient stub at `/{slug}/portal` is acceptable for this plan).

    Fill in `tests/fornecedor/signup.test.ts` Wave-0 placeholder per the 5 behavior tests. Use existing MSW BrasilAPI handler from Phase 1 for happy + degrade cases. Use tests/test-utils/dual-tenant.ts for the D-22 cross-tenant assertion.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/fornecedor/signup.test.ts --run</automated>
  </verify>
  <done>All 5 signup tests pass; signup-fornecedor.ts uses `actionClient` (not withTenantAction); vendor + member + consent rows all land per behavior; D-22 cross-tenant assertion green; CNPJ degrade-with-warning honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Marketplace list + event detail pages</name>
  <files>src/lib/actions/marketplace.ts, src/app/[slug]/marketplace/page.tsx, src/app/[slug]/marketplace/[eventId]/page.tsx, src/components/marketplace/event-card.tsx, tests/marketplace/list.test.ts</files>
  <read_first>
    - .planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-PATTERNS.md (lines 1170-1219 marketplace page analog — exact match with src/app/[slug]/eventos/page.tsx)
    - src/app/[slug]/eventos/page.tsx (page boilerplate analog — full file)
    - src/lib/actions/eventos.ts (listEventsInTenant pattern — full file)
    - src/db/schema/events.ts (events.status enum values — full file)
    - tests/test-utils/dual-tenant.ts (cross-tenant assertion helper — full file)
  </read_first>
  <behavior>
    - Test 1 (FORN-02 happy path): A fornecedor with active org membership for tenant_A visits /{slugA}/marketplace and sees only events of tenant_A with `status='published'`. Events with `status='draft'` are filtered.
    - Test 2 (cross-tenant): Same fornecedor (member of tenant_A only) tries /{slugB}/marketplace → 403 page (existing `activeOrganizationId !== tenant.id` guard from Pattern S9).
    - Test 3 (event detail): /{slug}/marketplace/{eventId} renders event name + dates + capacity + 'Ver planta' link to /{slug}/marketplace/{eventId}/planta.
    - Test 4 (RLS at DB): Direct DB query inside withTenant(tenant_A.id) lists tenant_A events; never sees tenant_B rows.
  </behavior>
  <action>
    Create `src/lib/actions/marketplace.ts` exporting pure helper `listOpenEventsInTenant(db, tenantId): Promise<MarketplaceEvent[]>`. Query: SELECT events.id, name, starts_at, ends_at, capacity, slug FROM events WHERE status='published' AND deleted_at IS NULL ORDER BY starts_at. Type the row narrowly — do NOT leak internal columns.

    Create `src/app/[slug]/marketplace/page.tsx` Server Component using Pattern S9 boilerplate from src/app/[slug]/eventos/page.tsx lines 25-74 verbatim — change `listEventsInTenant` to `listOpenEventsInTenant`. Add a top-level heading "Marketplace — {tenant.name}" + subtitle. Renders a grid of EventCard.

    Create `src/app/[slug]/marketplace/[eventId]/page.tsx` Server Component fetching one event via `getEventByIdInTenant` (existing helper from src/lib/actions/eventos.ts; if not present, add `getOpenEventByIdInTenant(db, tenantId, eventId)` to marketplace.ts that returns null if status !== 'published'). Render event detail card + "Ver planta" button linking to `/{slug}/marketplace/{eventId}/planta` (page lands in Plan 02-03 — link may 404 until then; that is expected).

    Create `src/components/marketplace/event-card.tsx` Client Component: card with name, date range (use existing date-fns formatter from src/lib/format/date.ts if present, else compose manually), capacity, button "Ver lotes disponíveis".

    Fill in `tests/marketplace/list.test.ts` per the 4 behavior tests. Use dual-tenant.ts for cross-tenant assertion.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_EVENTOS &amp;&amp; pnpm vitest tests/marketplace/list.test.ts --run</automated>
  </verify>
  <done>4 marketplace tests green; /[slug]/marketplace renders only published events; cross-tenant 403 guard active; event detail page links to /planta correctly (link may 404 — that is wired in Plan 02-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon-browser → /[slug]/fornecedor/cadastro | Untrusted form input; no session yet — verify slug-resolved tenant before any DB write |
| client → signupFornecedor Server Action | Untrusted email/password/CNPJ/phone/consent payload — Zod-validate at action boundary |
| anon → auth.api.signUpEmail | Better Auth handles password hashing; never bypass into a raw INSERT into user table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Spoofing | Signup form posts to forged slug | mitigate | resolveTenantBySlug returns 404 if slug unknown; auth.api.getFullOrganization re-checks org existence before addMember |
| T-02-02-02 | Tampering | Client toggles `payment_data` consent off but submits anyway | mitigate | Zod `consents.payment_data: z.literal(true)` blocks submit at server-action boundary |
| T-02-02-03 | Information disclosure | Cross-tenant event visibility via /{slugA}/marketplace from tenant_B member | mitigate | Pattern S9 activeOrganizationId vs tenant.id guard + RLS-FORCED at events table |
| T-02-02-04 | Repudiation | Fornecedor denies giving consent | mitigate | vendor_consents row + audit_log row with ip_address + consent_text snapshot (LGPD-04) |
| T-02-02-05 | Information disclosure | Brute-force email enumeration via signup | accept | Better Auth signUpEmail returns generic error; rate-limit not in Phase 2 scope; piloto Trindade has only fornecedores-in-the-know |
| T-02-02-06 | Elevation of privilege | Newly-signed-up fornecedor immediately gets `member` role | accept | D-23 = pending status; even with member role the vendor needs organizadora approval to be useful; pending vendors cannot reserve lots (Plan 02-03 guards) |
| T-02-02-SC | n/a | No new npm packages | n/a | Wave-0 placeholder mocks suffice |
</threat_model>

<verification>
- `pnpm vitest tests/fornecedor/signup.test.ts --run` green (5 cases)
- `pnpm vitest tests/marketplace/list.test.ts --run` green (4 cases)
- `pnpm test --run` baseline (181 + 9 = 190) all green
- `pnpm tsc --noEmit` 0
- `pnpm lint` 0
- Manual smoke: visit /trindade/fornecedor/cadastro → fill form → land on /trindade/portal (placeholder is OK)
</verification>

<success_criteria>
- FORN-01 + FORN-02 both green via test commands above
- New signup creates vendor + member + consent rows atomically
- Marketplace lists published events only; cross-tenant blocked at RLS + UI guard
- D-22 cross-tenant CNPJ works (same human → 2 vendor rows in 2 tenants)
</success_criteria>

<output>
Create `.planning/phases/02-fornecedor-self-service-checkout-pix-cartao/02-02-SUMMARY.md` when done.
</output>
